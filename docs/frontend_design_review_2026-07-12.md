# Frontend design review — 2026-07-12

> Historical design-review record. Physics-route statements below describe or
> quote the UI as it existed on 2026-07-12; they do not override the current
> standard OpenMC CE-fine/OpenMC MG-coarse rate-preserving SPH route in
> `PRODUCT_MODEL.md`.

Method: 47-agent workflow (9 page inventories → 6 design lenses → adversarial defense of all 30 cut/merge proposals → synthesis), plus interactive persona walks (J1 engineer, SPH physicist, cold evaluator). Verdict distribution on cut proposals: 28 modified into surgical versions, 1 cut stands, 1 feature kept outright. No route is cut.

# openmc2donjon Web Frontend — Design Review Synthesis

Answering the owner's three questions directly: **用户友好吗** — the working cores are excellent, but the app repeats itself between them (the same next-step link set renders up to five times after one convert). **需要这么多东西吗** — every route earns its existence; roughly a third of the *sections* do not. **流程合理吗** — the J1 spine is genuinely minimal (1 typed path, ~6 clicks to a runnable rdonjon command), but the product's own destination, DONJON, is missing from the nav, the homepage, and every pipeline model the UI draws. The fix everywhere is subtraction and re-labeling, never new features.

---

## 1. Verdict per route

| Route | Verdict | Reason |
|---|---|---|
| `/` (home) | **Keep, restructure chrome** | The four load-bearing blocks (hero definition, Start-here triage, Accepted validation, Backend health) are exactly the owner's bar; cut the third identical converter link (hero button), the "3 choices" badge, the demo sub-shortcuts, and replace the "Advanced tools" details (a More-menu duplicate that omits the one link that matters) with an "After you convert" block linking DONJON + Bundle. |
| `/convert` | **Keep, restructure the post-run report** | The input flow and toggles are the product's best work, but a successful run renders ~20 card sections repeating the same gates, checklist, CLI string, and link set — subtract to one verdict card + one delivery station + one collapsed evidence details. |
| `/openmc` | **Keep, restructure to one journey strip** | Legitimate home of J2/J3 and the crown-jewel SPH physics-summary card, currently buried under four misaligned retellings of a three-step story; also the only page whose "copy these in order" plan omits the commands that build its own prerequisite. |
| `/inspect` | **Keep, light restructure** | The J4 anchor with genuinely clean flow and load-bearing plots; fold the self-duplicating "Production hints" panel into the stat grid and — critically — give the page an exit (it is currently a dead end the whole app funnels into). |
| `/equivalence` | **Keep as-is structurally** (merge into /builder **rejected** after defense) | Its five mode-branched forms cannot be expressed by the generic builder engine without leaking stale hidden flags into copied commands; fix the make→inject handoff, the naming drift, and the buried 3-field happy path instead. |
| `/builder` | **Keep, cut the top-half chrome, give it a nav identity** | 12 of 14 forms earn their structure; the four stacked explanation panels before the first input do not, and bare `/builder` silently defaulting to diff should become a small command index. |
| `/commands` + `[id]` | **Keep the catalog, gut the chrome around it** | The 26-entry backend-served reference with per-card Copy CLI is real substance (per-card copy **stays** — defense confirmed the chained placeholder templates are the scripting user's feature); the three redirect devices, status-count tiles, coverage dashboard, and detail-page self-duplication go. |
| `/donjon` | **Keep, promote, trim the top third** | The only page serving J5 (deck skeletons, rdonjon commands, the validated 72-char and hex-VOID scars) is currently the least discoverable core page in the app; collapse the three guidance cards and provenance pills, promote to primary nav. |
| `/pygan` | **Keep in More** (fold-into-/convert rejected) | Sole executor of doctor and compare-writers with real PASS/FAIL evidence; delete the ASCII overview card, merge the availability chip into the Doctor panel, replace the broken "Doctor CLI" builder link with an inline copy button. |
| `/settings` | **Keep** (cut **rejected** after defense) | The prefix is a hand-authored directory that six pages consume and no point-of-use control can capture or clear; drop only the "Restore defaults" button (identical to clear+Save). |

No route is cut. The app's problem was never route count.

---

## 2. Target information architecture

**Primary nav (4):** `Convert | OpenMC prep | Inspect HDF5 | DONJON`
**More (5):** `Commands, Command builder, ADF/SPH sidecars, PyGan validation, Settings` (the separate "Bundle" row is dropped — bundle is a step reached from convert results and the builder index, not a destination).

- **`/`** — Triage which lane fits, show backend health and the accepted-validation credential; all six jobs reachable from the first screen.
- **`/convert`** — Turn an existing MGXS HDF5 into checked L_MULTICOMPO/L_MACROLIB ASCII (J1); production checks on by default.
- **`/openmc`** — Plan the OpenMC-side commands that produce the MGXS HDF5 and, when chosen, the CE/MG SPH evidence (J2+J3 planning); nav lands on export planning (`?workflow=two-step&production=1`, dropping only `equivalence=sph` — plain `/openmc` would silently disable production gates).
- **`/inspect`** — Read and verify any MGXS HDF5 down to per-mixture cross sections and scatter moments (J4), with exits to Convert and diff.
- **`/donjon`** — Generate the editable deck skeleton and run commands that consume the ASCII output (J5); nav description: "Generate the DONJON deck that consumes your ASCII."
- **`/equivalence`** — Build the five sidecar make/augment commands with mode-shaped forms (J3 decision surface).
- **`/builder`** — Fill inputs, copy the exact CLI for the 14 auxiliary commands (J3 prep, J5 packaging, J6); bare URL lands on a command index, not a silent diff form.
- **`/commands` (+`[id]`)** — Advanced reference: what each CLI command is for, its status, and where it runs on the web.
- **`/pygan`** — Optionally prove the ASCII and PyGan writers agree (J6).
- **`/settings`** — Default path prefix for path inputs and the file browser (nav description says exactly that).

---

## 3. Highest-leverage flow changes (ordered)

**1. Put DONJON at the end of every model of the flow.** Today the tagline is "for DONJON" yet the primary success card, the 5-stage delivery checklist, the home page, and the nav all terminate at Bundle or earlier; the one visible post-convert DONJON button sits mid-page in a rival card, and following it triggers an amber "manifest not found" warning for an optional step the user never took. Change: DONJON as 4th primary nav item; home "After you convert" block (DONJON + Bundle, replacing "Advanced tools"); a 6th terminal "DONJON" stage on the delivery checklist; gate the `manifest=` param in `convertDonjonGuideHref` on the existing probe result (or render a 404 as a neutral note). Cost: navigation.ts, one checklist item, one href guard — near zero.

**2. Collapse /convert's post-run report to two disjoint cards.** Two adversarial defenses pulled in opposite directions (merge everything into the outcome card vs. keep two cards); the reconciliation that honors both: card 1 (ConvertOutcomeSummary) = verdict + "Why this state" + Preview ASCII + Copy CLI only; card 2 (OutputActions, retitled **"Deliver to DONJON"**) = DONJON guide first, Bundle with the fork stated once ("The DONJON guide works from the ASCII path directly; bundle for the manifest-backed record"), file-status probe + Refresh + conflict badge. Zero overlapping buttons, page order verdict→gates→SPH→delivery preserved. Also delete: Gate highlights (gates render once in CheckCards, which auto-open on failure), ResultOverview, one of the two delivery-checklist renderings, the second BundleManifestProbe mount, the 01/02/03 stepper, the dead `canConvertNow` branch, and the intent banner for both `generic` and `direct-convert` intents. Render `convertPostWriteFocus`'s title/body (currently 90% dead code) as the station headline with DONJON as the default-branch primary. Cost: pure subtraction; roughly two viewports instead of seven, no substance lost.

**3. Make the production contract the default, not a query param.** Today `production` defaults to false and the strict preset survives only in 60-char curated URLs; two in-app links ("Open converter" on /pygan, "Back to convert" on /donjon) silently downgrade it, as does any bookmark. Change: `production=true` as the form default (URL params override for demo/SPH deep links), strip check/production from curated hrefs, and replace the four-checkbox row with a "Checks: Production / Standard / None" segmented control (the code already computes exactly this enum for its badge), nesting "Known mesh required" under it and moving "Overwrite" beside the Convert button. Cost: a form refactor plus tests; removes a silent safety downgrade and three fake decisions from the 90% path.

**4. Give /inspect and the ASCII preview exits.** /inspect emits zero outbound links although the whole app funnels into it, and the preview anchor strands users at page bottom below every next-step surface. Change: on inspection OK, one "Convert this HDF5" button (prefilled `?input=`, macrolib when SPH present) and a "Diff against a reference" link (`?command=diff&candidate_h5=`); on FAIL, keep the dead end (correct). End AsciiPreview with "Looks right — use it in DONJON." Cost: three conditional links.

**5. Rebuild /openmc around one journey strip and make its SPH plan complete.** Today four numbered systems disagree about what "01" means, and the SPH plan's "copy these in order" list fails at step 2 because the sidecar-building commands live elsewhere with only a collapsed panel as a bridge. Change: nav href drops `equivalence=sph`; Main line absorbs the demo card (its three prefilled deep links become per-phase actions — they are the only path that auto-loads the physics summary, so they must survive); delete the Workflow pipeline strip and the numbered steps grid; splice the three prerequisite sidecar commands into the plan when the sidecar readiness check fails, and link the failed row plus the sidecar field to `/equivalence?kind=openmc-sph-sidecar`; fix the status vocabulary at its source (`passed`→`planned`, `running`→`planning`, header `READY`→`PLAN READY`), keeping planned/blocked/optional. Cost: moderate; the deliverable (CLI commands) and crown jewel (SPH evidence) surface two panels sooner.

**6. Fix /equivalence's three self-inflicted traps.** Make→inject: seed `sphSource`/`adfSource` from the make tab's output on tab switch (~15 lines — the page's whole pairing currently forces hand-copying a path configured thirty seconds earlier). Naming: one convention end-to-end, `openmc_sph.*` (it is what the apply-sph spec already defaults to; `sph_sidecar.h5` collides with a different command), applied to canned CLIs, placeholders, and catalog strings, frozen with parity tests rather than a generator. Form shape: fold only Previous SPH + Damping behind an "Iterative update (review path)" disclosure and group the seven one-shot policy fields under a plain subheading — the rate-target and zero-flux controls are validated production prescription, not expert clutter, and must stay visible. Suppress the workflow panel's canned Copy-CLI on the active step so exactly one copyable version of the command being built is on screen.

**7. Strip the reference chrome on /commands and /builder.** /commands' first screen tells an evaluator "23 PARTIAL" and to leave, three times, before the first command card at four viewports down. Change: delete StatusCounts (one "Open converter" button in its slot), TaskLauncher, and the amber notice; keep the goal guide as screen 1 and the Workflow map as the single orientation device; one visible one-line status legend; rename the "Partial" filter to "Builder". Detail page: merge Use-when/Produces/Next-step + web mapping into one guidance block, delete Tags/bridge/goal-card re-renders, compress workflow position to a breadcrumb. /builder: delete CommandContextPanel (one compact "Produces: … · Next: …" line instead), the WorkflowHint strip, and the bundle 3-card strip (keep the prefill panel — it is the J5 hand-rail); mount the generic Before/After lane strip so the ADF chain gets 1-click hops without cloning the SPH showcase panel. Cost: deletion.

**8. Disclose mock mode symmetrically.** /convert — the page evaluators demo first — reports a fixture write as "created and confirmed on disk" while /pygan interleaves real environment data unlabeled. Change: a persistent "mock" nav chip when /health reports mock, a "mock fixture" stamp on the convert outcome badge (the pattern /pygan's result already uses), and mode-aware write copy. Cost: one chip, two strings.

---

## 4. Terminology normalizations worth doing

Adopt a shared vocabulary (a `lib/vocabulary.ts` or review grep). The domain terms (MGXS, CE/MG, SPH/NSPH, ADF/DF, L_MULTICOMPO, LCM, Legendre, statepoint) are already correct — the drift is entirely in the app's own coinages.

1. **handoff** = the MGXS HDF5 given to the converter, only. "ASCII handoff" → "ASCII output"/"DONJON ASCII"; "Bundle handoff" → "Bundle"; keep "HDF5 handoff" on /inspect, whose job is deciding whether the file *is* one.
2. **Three-term sequence system:** *workflow* = any ordered command sequence (matches the CLI noun); *route* = a mutually exclusive physics/consumption branch (keep "MACROLIB NSPH route", "OpenMC-side SPH route" — these are the validated credential); *path* = filesystem only ("Handoff path after this result" → "Next steps"). Drop lane/pipeline/chain/line as headings.
3. **augment / apply / never "corrected" for files:** tabs "Inject ADF/SPH" → "Augment ADF/SPH"; the augmented file is "SPH-augmented", apply-sph output is "SPH-applied"; keep "corrected" only for flux physics statements. One lock sentence on /equivalence: "Augment attaches factors as records — cross sections unchanged; apply-sph rewrites the cross sections."
4. **One label per flag, flag shown:** "Preflight (--check)" and "Production checks (--production)" on both /convert and /openmc; retire "gates" and "Validate first". Reserve "production" for the flag; the credential vocabulary is "Accepted validation" ("Live production minicase" → "Live minicase (validated reference)").
5. **bundle** = the one noun for the manifest-backed directory (verb: package); kill "delivery record", "managed handoff bundle", and friends; gloss once per page.
6. **Nav = stem of H1:** "Review HDF5" → "Inspect HDF5"; "PyGan option" → "PyGan validation"; retire the "planner" self-name outliers on /openmc; /equivalence H1 → "ADF/SPH equivalence sidecars".
7. **Jargon:** define "sidecar" once ("a small companion HDF5 carrying ADF/DF or SPH factors"); "smoke" → "smoke test" on first use; "for plumbing" → "for wiring checks only (no physics)"; "file-status probe" → "file check"; /openmc equivalence option "Direct" → "None (direct XS)".
8. **Micro:** "dry run" as noun, "dry-run" as modifier only (keep each panel's distinct second clause — do not merge the sentences); sentence-case the "Use when / Produces / Next step" trio; field label "Input HDF5" → "MGXS HDF5" on /convert.

---

## 5. Genuinely good — do not touch

- **The J1 spine:** path → auto-derived output → Dry run → Convert → auto-rendered preview → prefilled /donjon → copy deck. One typed path, ~6 clicks, zero forced decisions. Every change above is subtraction around it; none may add a step.
- **The honesty apparatus, verbatim:** the hero's "it does not solve reactor physics"; the SPH summary's "What it does not prove" and `applied_to_xs` footnote; the equivalence page's "factors are physics inputs… it does not judge whether the sidecar values are physically appropriate"; /donjon's "This page does not run DONJON"; the NSPH/MACROLIB consumption caveat everywhere it appears.
- **Accepted validation** on the home page — the exact evidence density this audience trusts, and the only place it lives.
- **The /donjon substance:** the state-aware checklist with the 72-char SEQ_ASCII and hex-VOID-only scars, the ingest-smoke-before-solve ordering, the deep-link parameter set and "Copy page link", and the page's own manifest probe with `donjon_defaults` auto-fill (a named fixed point — the convert-side duplicate probe is the cut, never this one).
- **Verification substance on /convert and /inspect:** the Production evidence strip, CheckCards gate details, full issue/warning lists, the cross-section plot, the scatter heatmap with moment refetch and its keep-last-good error recovery, the handoff signature + block scan.
- **The trust mechanics:** the file-status probe and its conflict state; CLI previews that provably match web runs (the unit-tested pygan parity pattern — extend it, don't replace it); argparse-dependency warnings; the "Preselected in web / You still provide" mapping on command detail pages; per-target extension filtering in the shared FileBrowserModal.
- **The demo card's inline-status pattern** on /convert — it is the model the rest of the report should imitate.
- **Every field-to-flag mapping** on /equivalence and /builder: nothing speculative survived inspection; the hint copy ("identity keeps previous SPH where both CE and MG flux are exactly zero") is expert writing worth protecting.

**Net effect if adopted:** same ten routes, same validated substance stated exactly once, roughly half the report surface and one screenful of chrome per secondary page removed, all six jobs reachable at their true rank, and the nav finally reads as the pipeline the hero describes: prep → convert → inspect → DONJON. That is 干干净净 applied to the map and the pages — without spending an ounce of the product's credibility.

## Appendix: defense-court verdicts

### MODIFY-PROPOSAL — The Bundle-vs-DONJON fork is unexplained, and the copy asserts Bundle as a sequential prerequisite it is not

Proposal: Reorder every action row DONJON-first after Preview, and disambiguate once, at the fork: rename the bundle affordance to 'Bundle (optional)' with body 'Package the record for delivery to another engineer — the DONJON guide works from the ASCII path directly.' Delete the 'then' sequencing from both headlines ('Preview the ASCII, use it in DONJON, and optionally bundle the record.').

Argument: The reviewer's diagnosis is confirmed by the product's own code — and it is drift, not doctrine: /donjon labels the manifest field "Bundle manifest (optional)" (app/donjon/page.tsx:319), and convertNextSteps already ranks "Use L_MULTICOMPO in DONJON" as a "ready" step 2 while Bundle is a "reference" at step 4, so the "Output ready" headline "Preview, bundle, then use in DONJON" and convertPostWriteFocus (badge "default production path" with primary="Bundle handoff", DONJON demoted to secondary) contradict the pages they link to. The prerequisite is even enforced mechanically, which the reviewer missed: convertDonjonGuideHref always appends manifest=<outdir>/bundle/manifest.json, so an engineer who goes straight to the guide is greeted on /donjon by an amber "Bundle manifest was not found" warning — the UI punishes the exact path /donjon declares optional. But the proposal overshoots in two ways. First, its relabel ("Package the record for delivery to another engineer") misstates a load-bearing feature: the bundle is the manifest-backed production record that /donjon's ManifestCasePanel actively consumes — auto-filling NMIX and the ASCII path from donjon_defaults, cross-checking summary/artifact mismatches, and surfacing the accepted-validation decision badge — so it serves the self-serve J1 user's audit trail, not just colleague handoff. Second, "reorder every action row" would break the one panel where the order is a true dependency: DeliveryCommandPanel ("delivery command chain", inside the collapsed "Advanced delivery evidence" details) says "Preview, bundle, then validate the manifest" — you cannot validate a manifest a bundle has not written, and DONJON-last is correct inside a delivery-scoped panel. Fix the fork on the primary surfaces and the amber punishment; leave the delivery chain and the bundle's substance intact.

Modified proposal: Four surgical edits instead of a blanket reorder+rename: (1) OutputActions.tsx "Output ready" card only — reorder buttons to Preview ASCII / DONJON guide / Bundle handoff / [Validate PyGan] / Inspect source; change the headline "Preview, bundle, then use in DONJON" to "Preview the ASCII, then use it in DONJON"; replace the body with the single fork sentence: "The DONJON guide works from the ASCII path directly. Bundle when you want the manifest-backed record — input HDF5, ASCII, run summary — for audit or handoff." (2) convertPostWriteFocus.ts default branch — swap primary/secondary (primary "Open DONJON guide", secondary "Bundle handoff") and retitle "Review the ASCII file, then prepare the DONJON input card", so the "default production path" badge points where the default path actually goes; leave the pygan branch unchanged (writer validation before delivery is genuinely recommended). (3) Stop the mechanical punishment — either omit manifest= from convertDonjonGuideHref unless the bundle manifest is confirmed (OutputActions already probes statuses.bundle), or downgrade /donjon's "missing" manifest state from amber warning to a neutral note: "No bundle manifest at this path yet — bundling is optional." (4) Do NOT rename the button to "Bundle (optional)" and do NOT reorder DeliveryCommandPanel: the fork sentence in (1) disambiguates once at the decision point, parenthetical button labels add the kind of noise the owner's bar forbids, and DeliveryCommandPanel's "Preview, bundle, then validate the manifest" is a true dependency chain inside an explicitly delivery-scoped advanced section.

### MODIFY-PROPOSAL — The execute-to-copy-CLI boundary is crossed silently at Bundle; button verbs are inconsistent about what happens on the other side

Proposal: Adopt one verb convention for every link that leaves web-execution for command-building: 'Prepare bundle command', 'Prepare validation command' (already correct), or a small 'CLI' chip on such buttons, applied uniformly across ConvertOutcomeSummary, OutputActions, the delivery checklist, and handoffActions. Rule: buttons named with a noun-result ('Bundle handoff') must execute; buttons that open builders must say 'Prepare … command'.

Argument: The bundle handoff link earns its place exactly as a link-to-builder: `bundle` writes a delivery directory on the local filesystem, which the product's execution boundary deliberately keeps out of the browser, and the click does real work — convertBundleHref() transfers output_dir/mgxs/mcompo/run_summary so the builder arrives prefilled ("Bundle builder is prefilled", "Fill inputs, copy CLI, run locally"), one copy-paste from done. The reviewer correctly spots a cleanliness defect — three labels for one destination on one page ("Bundle" ConvertOutcomeSummary.tsx:109, "Bundle handoff" OutputActions.tsx:324/convertPostWriteFocus.ts:43, "Open bundle builder" OutputActions.tsx:556, beside "Prepare validation command" :559) — but the verb rule mislocates the boundary: no navigation button executes on click ("Validate PyGan" also merely navigates; /pygan needs a further "Run compare" click), so click-effect cannot be the labeling criterion; destination capability can. The rule also skips "DONJON guide", the same modality flip it complains about, and would turn the job-named delivery route (Preview → Bundle → DONJON) into mechanism-speak. Keep job-first labels; mark the modality uniformly with the reviewer's own chip option.

Modified proposal: Converge on one canonical job-first label, "Bundle handoff", at all three sites (ConvertOutcomeSummary.tsx bare "Bundle", OutputActions.tsx DeliveryCommandPanel "Open bundle builder", convertPostWriteFocus.ts — already correct), and add a uniform small "CLI" chip (or trailing "· CLI" suffix) to every button whose destination is copy-CLI-only, applied by destination capability, not click effect: Bundle handoff, DONJON guide / Open DONJON guide, Prepare validation command (rename to "Validate bundle" + chip for grammar consistency), and the equivalence/builder deep links from convertNextSteps and the delivery checklist. Buttons whose destination executes in-app (Inspect source, Validate PyGan, Preview ASCII) carry no chip. This announces the execute-to-copy-CLI flip at the point of click while keeping the primary action row reading as the J1 delivery route rather than a command factory. Do not rename workflow steps to "Prepare … command".

### MODIFY-PROPOSAL — Post-convert, two rival green action cards force the user to diff them to find the one new action

Proposal: Merge: one post-convert action card = outcome verdict + Preview ASCII / DONJON guide / Bundle (optional) / Copy CLI + the artifact-status line with Refresh. Fold the conflict warning ('verify output path') into that card's badge, and demote everything else in OutputActions to the existing collapsed 'Advanced delivery evidence' details. This is the same subtraction the /convert reviewer proposes, restated from the J1 lens: one card, DONJON present, nothing to diff.

Argument: The overlap diagnosis is correct — in the converted state, "ASCII handoff ready" (Preview ASCII / Copy CLI / Bundle / Validate PyGan) and "Output ready" repeat four affordances and the user really does diff two green cards to find "DONJON guide" and "Copy DONJON path". But merging them into one top card is the wrong subtraction, for three code-verifiable reasons. (1) The page order is load-bearing: ConvertReport.tsx renders Outcome → "Validation summary" (whose Warnings tile says "Review before delivery" and whose "Gate highlights" are billed "contract + physics") → the "OpenMC-side SPH delivery" card (with its own "Next action" tile) → OutputActions. Delivery actions sit BELOW the gates and the SPH-sidecar verdict on purpose; a merged verdict+Bundle+DONJON card at the top invites shipping before the audit evidence the product exists to enforce. The actual flow bug is that card 1's Bundle button already jumps that queue — the fix is to remove it from card 1, not to promote the whole delivery station above the gates. (2) The two cards have different truth lifetimes: ConvertOutcomeSummary is a pure, immutable render of what the run reported; OutputActions owns the live disk probe (fetchConvertArtifactStatuses + "Refresh file status"), and convertOutputPresence exists so the probe can downgrade to "conflict" and gate bundling ("Bundling waits until the file-status probe can see the written ASCII output"). Folding the probe badge into the verdict card yields a "PASS / ASCII written" card that mutates under the user — worse for engineers on shared filesystems and in --mock demos. (3) The merge is three times bigger than stated: OutputActions is a three-state machine — BlockedOutputActions carries the "first fix"/"recommended next step" triage and DryRunOutputActions the write-target + "Convert now" — mirrored by convertDecision's three tones in card 1. Merging only the converted state leaves identical rival-card duplication in blocked and dry-run; merging all three dissolves a 900-line component and its probe machinery into the outcome card. The right change keeps both cards but makes their affordances disjoint by role, which achieves "nothing to diff" without breaking verdict→evidence→delivery.

Modified proposal: De-duplicate by role instead of merging. Card 1 (ConvertOutcomeSummary, converted state): keep PASS/verdict, "Why this state", Preview ASCII (the one J1 next step — a review action, consistent with reading before delivering) and Copy CLI (the reproducibility record of this run); delete its Bundle and Validate PyGan buttons. Card 2 (ConvertedOutputActions): make it unambiguously the delivery station — retitle the green card from "Preview, bundle, then use in DONJON" to "Deliver to DONJON", drop its duplicate Preview ASCII and Copy CLI, keep Bundle handoff (primary, matching convertPostWriteFocus), DONJON guide, Validate PyGan (pygan runs only), Inspect source, Copy DONJON path, the ASCII-handoff and Bundle-directory path tiles, and the file-status line with Refresh plus the "verify output path" conflict badge (fold the conflict downgrade into that card's badge as the reviewer suggests — it already is). Move the speculative "Manifest after bundle" tile into the already-collapsed "Advanced delivery evidence" details. Result: zero overlapping buttons between the two cards, each card answers one question ("did the run pass?" vs "how do I deliver it?"), page order gates→SPH→delivery is preserved, and the blocked/dry-run states need no surgery.

### MODIFY-PROPOSAL — After a passing dry run there are up to four 'Convert now' affordances

Proposal: Keep exactly two: the form's Convert button (persistent) and the outcome card's 'Convert now' (contextual, at the point where the user reads the verdict). Remove the copies in DryRunOutputActions, DeliveryCommandPanel, and the checklist item action.

Argument: The count is inflated and the cut targets the load-bearing instance. On a passing dry run only THREE convert affordances are visible, each about a screen apart: the form's persistent 'Convert', the verdict card's 'Convert now', and DryRunOutputActions' 'Convert now'. The claimed fourth and a sixth (ChecklistAction in DeliveryChecklist.tsx) are 11-12px text links inside <details> panels that are collapsed on a passing dry run ('Advanced dry-run evidence'; 'Run details and delivery checklist' has open={!data.ok}), where every sibling checklist cell carries its own inline 'Jump'/'Open' action — the 'ready' ASCII cell without its action would be the lone cell naming a next step it doesn't offer. The claimed fifth (DeliveryCommandPanel's canConvertNow branch) is unreachable dead code: onConvert is never passed at either call site (OutputActions.tsx:222, :362), and convertOutputMode routes every ok+dry-run+no-file result to DryRunOutputActions, so the rendering branches can never satisfy the condition. Most importantly, the button the proposal removes — DryRunOutputActions' — is the one the product's own philosophy makes primary: the hint 'a dry run gives a readable no-write record first' defines a reading path (verdict → validation gates → SPH handoff → 'Dry-run next step'), and this button sits at the END of that record, in the 'write target' sub-card beside the exact path it will write, under copy stating 'Convert is the only main action left before preview or bundle become relevant.' Cutting it keeps Convert where the engineer hasn't yet reviewed (top verdict) and deletes it where they have finished reviewing, forcing a two-screen scroll back — inverting the two-phase design for the careful reactor-physics persona it was built for. The three visible buttons map cleanly to three real postures: direct convert (form, explicitly sanctioned by the hint copy), quick accept at PASS (verdict card), and review-then-write (end of record).

Modified proposal: Keep all three visible primaries (form 'Convert', verdict card 'Convert now', DryRunOutputActions' 'Convert now') and both collapsed-panel text links (DeliveryItemAction in OutputActions.tsx, ChecklistAction in DeliveryChecklist.tsx). Delete only the dead code: DeliveryCommandPanel's canConvertNow logic in web/components/convert/OutputActions.tsx — the `canConvertNow` const (line 491), its cyan tone/heading/body branches (lines 502, 515-518, 523-527), the unused onConvert prop, and the never-rendered 'Convert now' button (lines 567-571). This removes the reviewer's 'fifth affordance' as a code-hygiene fix with zero user-visible change, since it can never render.

### MODIFY-PROPOSAL — The SPH journey is drawn four times with mutually misaligned step numbering — it reads as four route maps, not one journey

Proposal: One canonical route strip. Promote the 6-step command map to be THE strip (uncollapsed in SPH mode), give it the Main line's live status coloring and the demo card's 'Fill with demo' as a single chip on it, and delete the Main line 3-card grid, the demo card's 3-card grid, the post-plan 'Workflow pipeline', and the numbered steps grid (their per-step text merges into the CLI commands' descriptions, which OpenmcCommandList.tsx already hand-writes). Every panel then shares one numbering: the numbering of the commands the user will actually run.

Argument: The diagnosis (four misaligned numbered sequences) is correct, but the 6-step command map is the wrong canonical spine and the deletions cut load-bearing substance. (1) The demo card's three tiles are prefilled deep links, not a map: its evidence link's ?summary= param is the ONLY path that auto-loads physics_summary.json (OpenmcSphPhysicsSummaryCard.autoLoadPath fires only from the URL; 'Fill SPH planner' sets the path without loading), and its convert link carries the corrected minicase HDF5 + writer_backend=ascii into /convert — collapsing them to one 'Fill with demo' chip destroys the one-click demo/sales journey. (2) The Main line's SPH step 03 shows the validated MULTICOMPO caveat ('DONJON NCR: does not consume NSPH records from L_MULTICOMPO — use MACROLIB … or pre-apply with apply-sph') PRE-plan, at the moment the user picks the format toggle; merging step text into OpenmcCommandList descriptions defers it to after a successful plan, past the choice it guards. The Main line is also the only route strip for direct/ADF/flux-ratio modes. (3) Promoting the 6-step map elevates '04 Optional damped rerun' — which the same file marks 'damping-sensitive… not the default production claim' — into a numbered main-line step, contradicting the accepted one-shot-SPH physics; and per-command status coloring would assert execution state the page cannot observe (it executes nothing; the Main line's 3 phases are the honest status granularity, per its own code comment 'a successful plan only means the commands were planned'). The map is a shared you-are-here reference on /equivalence and /builder; its <details> home on /openmc is correct placement, not accidental burial. The real fix is two panels at two altitudes — one journey strip, one collapsed command reference — with the demo grid and the post-plan duplicates removed.

Modified proposal: Reduce four numbered systems to one journey + one reference. (a) Keep OpenmcProductionPathPanel ('Main line') as THE journey strip; merge the demo card into it: when a demo preset exists, put 'Fill SPH planner' in the strip header and attach the demo's three deep links as per-phase actions (evidence href on phase 02 'Review production evidence', prefilled convert href on phase 03, bundle href in the existing post-plan action row) — delete OpenmcSphMainlineCard's separate 3-card grid, eliminating the demo-01-vs-mainline-02 collision by construction. (b) Trim the status vocabulary to what the page can honestly assert about planning (needed / ready / planned / blocked); drop passed, running, optional. (c) Keep the 6-step command map collapsed in 'Detailed OpenMC SPH command map' but drop its 01–06 numerals in favor of its existing CE/MG/SPH/OPT/HDF5/ASCII badges, so /openmc has exactly one numbered sequence (it stays numbered-free on /equivalence and /builder too, where the active highlight already orients). (d) Post-plan, the reviewer is right: delete the numbered plan.steps grid (duplicates OpenmcCommandList one-for-one) and the 'Workflow pipeline' section of OpenmcWorkflowSummary (its three stage paths already appear in OpenmcArtifactList with Inspect/Copy); keep the four summary tiles, CLI commands, Readiness, Artifacts, and Next actions.

### MODIFY-PROPOSAL — The planner is the right abstraction but wears an execution-tracker costume that over-promises: 'passed', 'running', 'READY' for a page that runs nothing

Proposal: Keep planning + preflight + handoffs untouched; shrink the vocabulary to what a planner can truthfully say: form-level 'needed / ready', readiness-check 'pass / warn / fail' (already the backend's words), and one post-plan verdict 'PLAN READY / NEEDS INPUT'. Delete 'passed', 'running', and the patch that converts one into the other. Rename the idle card's promise to match: it already has the right sentence ('This planner does not execute OpenMC. It gives you the exact CLI commands and artifact map') — make the rest of the page as honest as that line.

Argument: The reviewer diagnosed the right disease at the wrong severity and prescribed an overdose. Verified against the code: 'passed' is unreachable in the UI — its sole consumer (OpenmcProductionPathPanel.tsx:75) remaps it to 'planned' before render and the emerald 'passed' CSS branch (line 242) is dead code, so the claimed user-facing over-promise ('green passed chips') does not exist on screen; the bug-fix campaign already fixed the user, leaving only source-level vocabulary debt. The single genuine user-visible slip is 'running': during the plan HTTP fetch it renders on the SPH card titled 'Run OpenMC CE/MG SPH' / eyebrow 'Run physics', briefly claiming physics is executing. Meanwhile the proposal's enumerated replacement vocabulary (needed/ready, pass/warn/fail, PLAN READY/NEEDS INPUT) silently omits 'planned', 'blocked', and 'optional' — the panel's load-bearing states: amber 'planned' is the future-tense honesty the reviewer wants ('will exist after you run the commands'), rose 'blocked' propagating to all three cards on a failed plan (openmcWorkflowWalkthrough.ts:53-55, tested) is the preflight's teeth telling engineers not to paste commands from a failed plan, and readiness cards already render the backend's pass/warn/fail verbatim (page.tsx:805-810). So: keep the status model, fix the vocabulary at the source instead of the consumer, and rename only the two words that can actually mislead — a cleanup, not a structural redesign.

Modified proposal: Fix the vocabulary at its source instead of shrinking it, keeping planned/blocked/optional and the failed-plan propagation intact: (1) In lib/openmcWorkflowWalkthrough.ts, make openmcWalkthroughStatuses return 'planned' (not 'passed') for a successful plan and rename the loading value 'running' to 'planning'; delete 'passed' and 'running' from the OpenmcWalkthroughStatus union. (2) In OpenmcProductionPathPanel.tsx, delete the line-75 patch and its apologetic comment (now unnecessary), delete the dead emerald 'passed' branch in openmcWalkthroughStatusClass, and move 'planning' into the cyan branch. (3) In app/openmc/page.tsx line 593, change the plan-header verdict 'READY' to 'PLAN READY' (keep 'NEEDS INPUT'). (4) Update openmcWorkflowWalkthrough.test.ts:96 to assert plan: 'planned' — the test's own name ('marks a successful plan as ready to copy and run') already describes the honest semantics. (5) Leave untouched: needed/ready form-level statuses, the blocked propagation on failed plans, the 'optional' bundle state, the backend pass/warn/fail readiness chips (already rendered verbatim), and the idle card's 'This planner does not execute OpenMC' copy.

### MODIFY-PROPOSAL — /equivalence 'OpenMC CE/MG SPH options' buries the accepted one-shot route's 3-field happy path under 9 uncollapsed expert knobs, while two trivial flags get the disclosure treatment

Proposal: Move the nine iteration/robustness fields behind a disclosure titled 'Iteration and robustness (review path)' mirroring the existing 'Common CLI options' pattern; keep every field and every hint (the hint copy is excellent and validated — no cuts). The default view becomes CE flux, MG flux, table output: a 3-field form matching the accepted evidence.

Argument: The proposal miscounts the "expert knobs": of the nine, only Previous SPH and Damping are iteration-exclusive. The amber caveat it cites governs optional damped OpenMC MG reruns (workflow step 04, apply-sph), not these fields. Six of the nine are one-shot production controls with validated standing — 'SPH target = rate' plus 'Freeze groups' IS the accepted core-closure prescription (the release is named v0.1.4-sph-rate; the freeze hint encodes the validated group-31 practice), and 'Zero-flux policy' / 'Flux floor' are mandatory on fast-spectrum cases: with the default reject policy ("fail on zero bins"), the proposed 3-field happy path fails at runtime on the product's own flagship IRENA-class case, with the remedy hidden inside a fold whose "review path" label tells that exact user it isn't for them. Folding these under "Iteration and robustness (review path)" would invert the validated record — labeling the production prescription as the non-default path. The real, fixable problem is scan structure, not field count.

Modified proposal: In OpenmcSphSidecarFields (app/equivalence/page.tsx), keep the core trio (OpenMC CE reference flux, OpenMC MG macro flux, SPH CSV table) as the first visible group. Fold ONLY the two genuinely iteration-only fields — Previous SPH and Damping — behind a details block titled "Iterative update (review path)", mirroring the "Common CLI options" pattern (folded ≠ removed: the CLI preview still emits --damping 1.0 explicitly). Keep the seven one-shot policy fields visible but grouped under a plain subheading such as "Target and spectrum policies" (SPH target, Zero-flux policy, Flux floor, Freeze groups, Flux normalization, Clip min, Clip max), so the one-shot user sees a labeled block they can consciously skip while the fast-spectrum and rate-prescription controls remain discoverable at first glance. All hint copy unchanged.

### MODIFY-PROPOSAL — /openmc entry-point cards are silent preset buttons dressed as navigation: 'Plan HDF5 export' mutates form state below the fold without moving the viewport

Proposal: Reuse the demo card's pattern: after applyEntryPoint, scroll to the form (or to the summary card for the SPH entry). Longer term, make the two entry cards the actual mode switch for the page and remove the now-redundant 'Workflow' segmented control from the form (its value is derivable per the matrix finding), so choosing a lane and configuring it stop being two disconnected UI systems.

Argument: The feedback gap is real and worse than stated — on default landing (one-step/direct) the first card is already chip-marked "selected", so clicking "Plan HDF5 export" produces zero visible change while silently flipping workflow and Production gates below the fold; the fix pattern (apply then scrollIntoView) already exists 20 lines away in reviewOpenmcSphDemo. But the longer-term half of the proposal is factually wrong: the Workflow control is not derivable from the two cards. The backend planner (src/openmc2donjon/web/openmc_workflow.py:579-594) treats one-step as a distinct lane — flux-ratio ADF is REJECTED under two-step with the message "Switch to one-step for flux-ratio ADF" — and from_openmc_sph.py/from_openmc_adf.py implement one-step SPH/ADF workflows. Both cards pin two-step, and Equivalence has four values against two cards, so making the cards the mode switch would delete reachable, backend-supported configurations (one-step direct, one-step+flux-ratio-adf). The cards themselves earn their place: they are the J2/J3 lane selector, encode validated presets (SPH forces macrolib, enables production+check, clears stale sidecar paths that would poison the next plan request), triage J1 users off the page, and their active state is derived from form state — synchronized with the form, not duplicating it.

Modified proposal: Adopt only the scroll fix, mirroring reviewOpenmcSphDemo in app/openmc/page.tsx: after applyEntryPoint, setTimeout-scrollIntoView to "#openmc-sph-summary" for the openmc-sph entry and to the form (give it an id, e.g. "openmc-planner-form") for direct-mgxs — or, equivalently, make the primary buttons navigate via the same query-param+hash mechanism the "Open SPH summary" secondary link already uses correctly, which adds URL shareability for free. Do NOT remove the form's Workflow segmented control or promote the cards to the page's mode switch: one-step is a real backend lane (required for flux-ratio ADF per openmc_workflow.py:579-587, and one-step SPH exists in from_openmc_sph.py) that neither card represents. Optionally restyle "Open SPH summary" from btn-secondary to a plain text link to stop it reading as cross-page navigation.

### MODIFY-PROPOSAL — Two contradictory recommended orders ship simultaneously (bundle-before-DONJON vs DONJON-before-bundle), and the module that encodes the order is 90% dead code

Proposal: Pick one canonical order and state it once. The physics-correct order for a single engineer is preview → DONJON ingest smoke (the /donjon checklist itself says 'Run the ingest smoke before the physics deck' — the cheapest proof the ASCII is consumable) → bundle only when delivering; bundling an unproven ASCII is packaging before QA. Encode that order in one module (a slimmed convertPostWriteFocus is the natural home), render it in exactly one card, and delete the unrendered fields or render them — a dead 'recommended order' module is how the contradiction crept in.

Argument: The proposal's diagnosis is half right but its prescription would break a real, code-backed data flow. The canonical order is not up for grabs: the bundle manifest is upstream INPUT to the /donjon guide, not downstream packaging — app/donjon/page.tsx auto-fills the ASCII path, format, and the deck skeleton's NMIX from the manifest's donjon_defaults (emitted by web/bundle.py for exactly this purpose), and convertDonjonGuideHref pre-wires the predicted manifest.json into the guide URL. Bundling first makes the ingest smoke MORE likely to pass, and the /donjon line the reviewer quotes ("Run the ingest smoke before the physics deck") orders smoke vs. physics deck within DONJON work — it says nothing about bundle-vs-DONJON, while the adjacent 72-char "stage the ASCII on a short path" caveat shows the bundle is how the ASCII travels to where DONJON runs. Moreover, three of the four order statements already agree (the "Output ready" headline, DeliveryCommandPanel, and — ironically — convertPostWriteFocus's own unrendered title "Review the ASCII file, bundle it, then open the DONJON guide" state the SAME preview→bundle→DONJON order); only convertNextSteps deviates, and both dissenters sit inside collapsed <details>, so "two contradictory orders ship simultaneously" overstates the exposure. The real defects are (a) one outlier module and (b) genuinely dead fields — only focus.badge renders at OutputActions.tsx:305. Fix those; don't flip the physics of the flow.

Modified proposal: Keep preview → bundle → DONJON as the single canonical order (it is data-flow-backed: bundle emits manifest.json with donjon_defaults that /donjon consumes to prefill ASCII path, format, and NMIX). Three changes: (1) In lib/convertNextSteps.ts converted branch, move the bundle step ahead of the DONJON step and promote it from status "reference" to "ready" (it is a prefilled one-click builder and the headline's recommended second step), so the collapsed "Production handoff next steps" grid agrees with the "Output ready" card. (2) Make convertPostWriteFocus the single source for the focus card headline: render focus.title and focus.body in ConvertedOutputActions in place of the hardcoded "Preview, bundle, then use in DONJON" h4/paragraph — this resurrects the module's real payload (the pygan branch's "Validate the PyGan writer against the default ASCII writer" guidance, currently reduced to a badge) and makes the headline backend-aware. (3) Delete the now-redundant primaryLabel/primaryHref/secondaryLabel/secondaryHref fields and their tests — the existing button row already covers both branches via the conditional "Validate PyGan" button. No new module, no reordering of the product's delivery flow, and the /donjon smoke-before-physics-deck caveat stays untouched where it belongs.

### MODIFY-PROPOSAL — J5 is split across two sibling nav concepts, 'Bundle' and 'DONJON', when bundle is a step, not a destination

Proposal: Collapse to one nav concept. Either (a) one More item 'Deliver to DONJON' → /donjon, with the bundle builder linked from /donjon's manifest section ('No bundle yet? Package this run') and from convert results — dropping the standalone Bundle item; or (b) keep both but make the pairing explicit in the descriptions ('Bundle: package a run for delivery — the recipient opens it on the DONJON page'). Also add one sentence to the /builder bundle prefill panel telling the sender what to send: 'Give your DONJON colleague the bundle directory and this guide link.'

Argument: Bundle and DONJON are not "one job split in two" — they are two jobs with different actors that only sometimes compose, and the code already wires the composition at the point of action rather than in the menu. (1) /donjon is used without any bundle: its manifest field is literally labeled "Bundle manifest (optional)", and every successful conversion's "What happens next" links straight to "Use L_MULTICOMPO in DONJON" via convertDonjonGuideHref (convertNextSteps.ts:49) with ascii/format/NMIX/manifest-path pre-threaded — the common single-machine engineer (same person does lattice and core) never bundles. (2) Bundle is used without DONJON: its manifest carries check/doctor/diff/inspect summaries (builderPrefill.ts ARTIFACT_FIELDS) — it is the production audit record ("Package the production record", convertNextSteps.ts:195), a QA deliverable in its own right. (3) The pairing the reviewer says is missing already exists bidirectionally where a user acts: the bundle prefill panel renders an "Open DONJON guide" button whose href carries manifest+ascii+format+deck filename (builderPrefill.ts:87-105), its step 03 says "Send the bundle to DONJON users, or run validate-bundle before delivery", and /donjon links back with "Validate bundle" (donjon/page.tsx:338-347). Proposal option (a) is therefore wrong twice over: it mislabels /donjon as "Deliver" when the page's own header says "This page does not run DONJON… it turns the converter output path into deck fragments" (it's a consumption guide, not a shipping step), it puts the audit-record job behind a deck-guidance page that job doesn't need, and it mechanically orphans nav state — the Bundle item's match: ["/builder"] (navigation.ts:43) is the only thing that highlights ANY /builder?command=* page (diff, validate-bundle, all sidecar builders); dropping it de-anchors a dozen pages. What IS real and cheap to fix is the residual copy gap: the two More-menu one-liners don't state the relay, and the sender is never told the recipient's entry point is /donjon+manifest. Fix the three strings, keep the structure.

Modified proposal: Keep both nav items and the current page structure; make the relay explicit in three strings. (1) In web/lib/navigation.ts, Bundle description: "Package a run for delivery or audit — recipients open the bundle on the DONJON page." (2) DONJON description: "Turn an ASCII handoff or received bundle into deck skeletons and pre-run checks." (3) In web/app/builder/page.tsx, BUNDLE_WORKFLOW_STEPS step 3 ("Share or validate") body: "Send the bundle directory to DONJON users — the DONJON page reads its manifest.json to prefill the ASCII path, format, and NMIX — or run validate-bundle first." Do not collapse the nav items (Bundle's match:["/builder"] is the nav anchor for every builder page) and do not add a bundle CTA to /donjon beyond the existing "Validate bundle" link — /donjon is the recipient/consumption side and already handles the no-bundle case by design.

### MODIFY-PROPOSAL — The recommended-order copy that does exist is good — protect it while consolidating

Proposal: Treat these as fixed points when applying the other findings: consolidation should delete duplicate renderings (second manifest probe, contradictory order statements, checklist double-mount), never the /donjon checklist items, the smoke-first sequence, or the deep-link parameter set.

Argument: Every claimed fixed point checks out in code, and each carries substance that exists nowhere else in the UI: the 72-character SEQ_ASCII truncation and the SNT hex VOID-only boundary scar appear only in donjonDeckChecklist (lib/donjonGuide.ts:205,213) — grep confirms no other web-visible copy carries them — and the checklist interpolates live form state (NMIX, geometry, boundaries, solver), so it cannot be replaced by static docs; the UTL:DUMP ingest smoke (donjonIngestOnlySnippet) is rendered before the solve skeleton with "Use this tiny deck first" copy, which is the cheapest consumption proof an engineer can run; and convertDonjonGuideHref → donjonDeckOptionsFromSearchParams → "Copy page link" round-trips the whole configured guide, making the convert→DONJON hop and colleague handoff zero-typing. The deletion targets are equally real: ConvertReport mounts convertDeliveryChecklist twice (OutputActions' DeliveryPathStrip + ConvertRunDetails' DeliveryChecklist), OutputActions probes "Manifest after bundle" before any bundle can exist, and its "Preview, bundle, then use in DONJON" heading contradicts convertNextSteps' DONJON-ready-before-Bundle-reference ranking. But the proposal underspecifies its own knife: the repo has exactly two inspectBundle probes, and only convert's BundleManifestProbe is the duplicate — /donjon's probe (app/donjon/page.tsx:208 + ManifestCasePanel) is the receiving end of the protected `manifest` deep-link parameter, auto-filling ASCII/format/NMIX from donjon_defaults and raising the summary/artifact mismatch warning. A consolidator reading "delete the second manifest probe" could sever the very chain the proposal protects, so the /donjon manifest hydration must be named a fourth fixed point.

Modified proposal: Treat these as fixed points when applying the other findings: (1) the /donjon checklist items in donjonDeckChecklist, including the 72-char SEQ_ASCII scar, the SNT hex VOID-only boundary scar, and their live-form-value interpolation; (2) the ingest-smoke-before-solve sequence (donjonIngestOnlySnippet card rendered first); (3) the deep-link parameter set (ascii/format/manifest/deck/nmix plus deck options) and "Copy page link" serialization; (4) — added — the /donjon page's own manifest probe and ManifestCasePanel (donjon_defaults auto-fill of ASCII path/format/NMIX, summary-vs-artifact mismatch warning), which is the consumer of the deep link's manifest parameter and the only bundle-first entrance to J5. Consolidation deletes only the convert-side duplicates: the BundleManifestProbe inside OutputActions' "Advanced delivery evidence" (it probes "Manifest after bundle" before any bundle exists), the double rendering of convertDeliveryChecklist (keep one of DeliveryPathStrip / DeliveryChecklist in ConvertReport's tree), and one of the two contradictory order statements — align OutputActions' "Preview, bundle, then use in DONJON" heading with convertNextSteps' actual ranking (Preview → DONJON ready, Bundle reference) or vice versa, but tell one story.

### MODIFY-PROPOSAL — Two parallel form-builder surfaces for one job: merge /equivalence's five forms into /builder's spec engine

Proposal: Add a `visibleWhen: {field, equals}` property to BuilderField (one small extension to lib/commandBuilder.ts), port the five equivalence kinds into COMMAND_BUILDER_SPECS, and make /equivalence a thin route that mounts the generic builder with its 5-tab kind strip preserved (or redirect ?kind=X to /builder?command=Y keeping the tab strip on /builder for equivalence commands). Delete equivalenceCommand.ts, equivalenceKindSwitch.ts, and the 849-line page. The make→inject handoff then becomes a prefilled 'Inject this sidecar' link (output of the make form → adf_source/sph_source query param), which the existing builderValuesFromQuery already supports with zero new machinery. End state: exactly one form surface, one CLI assembler, one shellQuote, one copy button per command — with equivalence keeping its tab navigation, its per-kind summaries, and the load-bearing 'physics inputs' amber caveat as spec notes.

Argument: The merge treats an import-graph observation as a UX defect. Users never see "two form engines" — they see two consistent pages (same CLI-preview aside, same copy button, same file browser), and the owner's "干干净净" bar is about the user's glance, not code sharing. What users DO see on /equivalence is design the generic builder cannot render: full-width Input/Output fields up top, boxed per-kind sections ("ADF sidecar options", "OpenMC CE/MG SPH options" with its physics prose describing a heterogeneous CE fine reference, a homogenized MG coarse calculation, and their declared conservative comparison-domain map), and a collapsed "Common CLI options" details block — versus /builder's flat uniform 2-column card grid, which would turn the flagship production form (make-openmc-sph-sidecar, 12+ mode-shaped fields, the v0.1.4 SPH centerpiece) into an undifferentiated wall. The "one small visibleWhen extension" also materially understates the port: buildCommandCli (commandBuilder.ts:472-503) emits every non-empty field unconditionally, so visibility-gating without emission-gating/clear-on-hide lets stale hidden values leak — type a clip-min in flux-ratio mode, switch to unity, and the copied command silently carries --clip-min the form no longer shows. buildAdfSidecarCli/buildSphSidecarCli/buildOpenmcSphSidecarCli (equivalenceCommand.ts:162-250) encode exactly these rules today: mode-scoped optional flags, always-emit --mode, suppress-default --sph-target/--zero-flux-policy. Re-deriving five freshly-validated assemblers (locked by equivalenceCommand.test.ts) inside a spec engine that must grow conditional emission, grouping, prose, and live-state-carrying tabs — immediately after the 58-defect stabilization — is the expensive, risky path to fix three things that are each one surgical change. The reviewer's two live defects are real (make→inject forces hand-copying the output path; make-sph-update-table has no workflow inbound link) and the shellQuote clone is real; none requires deleting the page.

Modified proposal: Keep /equivalence and its five hand-rolled forms; land three targeted fixes instead of the merge. (1) Make→inject handoff: extend equivalenceOptionsForKindSwitch (lib/equivalenceKindSwitch.ts) so switching from a make kind to its inject sibling seeds the sidecar source from the outgoing tab's output path — adf-sidecar→augment-adf seeds adfSource, openmc-sph-sidecar/sph-sidecar→augment-sph seeds sphSource — or, if implicit carry-over feels surprising, add an explicit "Next: inject this sidecar" link in the make tabs' CLI-preview aside that navigates to the inject tab with the output path as a query param (add a small parse step mirroring builderValuesFromQuery). ~15 lines plus tests either way. (2) Route the stranded sibling: on step 03 of OpenmcSphWorkflowPanel (and/or in the "OpenMC CE/MG SPH options" section) add a secondary link "CSV-only iterative table: make-sph-update-table" → /builder?command=make-sph-update-table, with copy clarifying they are different stages (one-shot sidecar for augment-sph vs update table for the apply-sph loop), not the same command in two formats. (3) Extract the character-identical shellQuote (commandBuilder.ts:658-661, equivalenceCommand.ts:281-284) into a shared lib/shellQuote.ts imported by both; mechanical and covered by both existing test suites. Explicitly do NOT port the five kinds into COMMAND_BUILDER_SPECS: visibleWhen without emission-gating leaks stale hidden values into copied commands, and the port trades five validated per-kind CLI assemblers plus the page's curated section anatomy for new generic-engine complexity that serves only these five commands.

### MODIFY-PROPOSAL — 9 of 14 builder specs have no journey link; 6 are pure catalog orphans — route the ADF chain, drop doctor/serve specs, resolve the SPH-table sibling

Proposal: Split by case. (a) ADF chain: render an AdfWorkflowPanel on /builder for the five ADF-prep command ids, exactly the isOpenmcSphWorkflowCommand pattern — the lane data already exists in COMMAND_WORKFLOW_LANES 'adf-df' (commandWorkflowLanes.ts:80-117), so this is mostly a mount, and it converts five orphaned specs into a supported J3 route with step-to-step prefill. (b) make-sph-update-table: either delete the spec (make-openmc-sph-sidecar already writes the CSV via --table-output) or link it from the SPH panel's step 03 as the table-only variant; today it exists as a fully built 12-field form nobody can find. (c) doctor and serve: drop the builder specs and let the catalog fallback's static CLI copy serve them — doctor's only non-trivial flags are covered by builderCliIssues theater for a command engineers type by hand, and building the serve command inside the UI that serve itself hosts is only ever useful for --mock restarts, which the Backend card's error copy already provides verbatim. Keep both catalog entries.

Argument: The proposal's factual premise — "6 pure catalog orphans" — fails code inspection on three counts, and its one cut is aimed at the two specs that must survive the failure mode they exist to fix. (1) The "unroutable chips" claim is wrong: every CommandChip is a Link to /commands/[id] (CommandPrimitives.tsx:9), and each detail page deep-links its builder via "Open web workflow" (web_path, [id]/page.tsx:118) plus a "Workflow position" card with Before/After step navigation from commandWorkflowOccurrences — so make-low-order-driver, check-low-order-driver and make-homogeneous-face-flux are two clicks from the ADF lane, not orphaned. (2) make-sph-update-table is already resolved as the table-only sibling: isOpenmcSphWorkflowCommand special-cases it (openmcSphWorkflow.ts:112-117), so its builder renders the full 6-step SPH panel with step 03 "Compute SPH factors" highlighted active (line 130-132), and the SPH lane chips it explicitly (commandWorkflowLanes.ts:74-76); deleting it also isn't the frontend's call — the CLI ships the command with --previous-sph/--damping iteration semantics, and the web's contract is to mirror the catalog. (3) Dropping the doctor/serve specs and "letting the catalog fallback's static CLI serve them" is structurally impossible: the catalog is served by the FastAPI backend, and builderFallbackCopy deliberately returns cli:null on catalog error ("no CLI preview is available", builderCopy.ts:37-44). serve and doctor are exactly the commands you need when the backend is down or broken ("Use this when a collaborator reports that local commands fail to start") — the client-side specs are the only surface that still renders in that state. They are also not filler: J6 (pygan, serve, doctor) is a named product job; doctor's --statepoint⇒--recipe dependency is the motivating case for builderCliIssues (commandBuilder.ts:505-510) and its doctor_summary.json feeds bundle's --doctor-summary field in the delivery evidence chain. What survives of the proposal is the real asymmetry in (a): mid-ADF-chain on /builder, the next step is 3 clicks away versus SPH's 1-click "Open step". But cloning the SPH panel is the wrong fix — that panel's substance is owner-validated accepted-evidence copy ("one-shot two-region production probe") and prefilled tally/dataset names; a look-alike ADF panel would need new physics copy and would visually promote a prep chain to production-mainline status, against the owner's "nothing superfluous" bar. The minimal complete fix reuses machinery that already exists.

Modified proposal: Keep all 14 builder specs, including doctor and serve (client-side specs are the only surface available when the backend/catalog is down — the exact moment those two commands are needed — and J6 names them as a product job). Fix the one real gap, ADF step-to-step navigation, with the existing generic mechanism instead of a second showcase panel: on /builder, when commandWorkflowOccurrences(spec.id) is non-empty and the SPH panel is not already rendered, mount the same Before/After lane-position strip that /commands/[id] already renders (WorkflowOccurrenceCard + NeighborStep, [id]/page.tsx:504-568, extracted to a shared component). This gives export-surface-flux → check-face-flux → make-adf-sidecar 1-click chain hops, covers make-low-order-driver/check-low-order-driver/make-homogeneous-face-flux via their lane steps, and also benefits diff, bundle and validate-bundle — one mechanism, no new physics copy to review. For make-sph-update-table, change nothing structural (it already renders inside the SPH panel as active step 03 and is chipped in the SPH lane); optionally add a one-line "table-only variant" link next to the table-output field on /equivalence?kind=openmc-sph-sidecar for outward discoverability. Do not delete any spec; whether make-sph-update-table is redundant with make-openmc-sph-sidecar --table-output is a CLI-catalog question for the owner, not a frontend cut.

### MODIFY-PROPOSAL — /commands should be a compact index, not a third full-depth surface — its unique substance is backend status truth and per-command guidance, both of which fit the detail page

Proposal: Index: header sentence (the existing 'Advanced reference… For normal work, start with Convert' already does the redirect job alone) + search + group filter + compact rows (mono name, status badge, one-line summary, Open). Keep the WorkflowMap as the single orientation device since it is shaped like the product pipeline; delete StatusCounts, TaskLauncher, the ReferenceNotice buttons, the goal-card guide, and the coverage dashboard (keep the ready/partial/CLI-only legend as badge tooltips). Drop static CLI + Copy from index rows — the copyable command lives on the detail page and the buildable one on /builder. Detail page: header + merged guidance block (Use When/Produces/Next Step + web mapping, stated once) + CLI form + a one-row workflow-position breadcrumb. On /builder, delete CommandContextPanel outright; the 'Command guide' button next to it is the honest version of the same information. Net: /commands stops being a 13k-px third surface and becomes the lookup it claims to be, with zero loss of the catalog's unique substance.

Argument: The redundancy case is verified in code — three redirect devices stacked on one index (header sentence, verbatim home TASK_ENTRYPOINTS, amber ReferenceNotice with nav-duplicate buttons), three taxonomies over the same 26 entries, next_step and mapping.summary each rendered twice on the detail page, and the direct-convert "Consumption rule" already living in convertShowcase.ts/convertArtifactAnatomy.ts and /donjon — so the compaction stands. But the proposal overshoots in exactly the places that serve the page's actual user, a CLI-first engineer: (1) dropping CLI+Copy from index rows mistakes content-identity for access cost — /builder shows one command per ?command=X navigation, while the index is the only surface where all 26 templates are visible and copyable at once; removing copy adds a page navigation to the reference's single most frequent operation (find → copy → edit paths in terminal) to save one line of mono text that fits a compact row anyway; (2) deleting CommandContextPanel outright from /builder removes the "Produces / After this" chaining hint at the moment of command assembly, which is what keeps the multi-command pipeline (export-volume-flux → make-openmc-sph-sidecar → augment-sph → direct-convert) navigable without a detour through the guide page — only its "Use when" row duplicates spec.summary; (3) tooltip-only status legend hides the one visible sentence stating the product's non-mutation safety contract ("the web prepares the command; production mutation stays in the CLI") on the page where a newcomer scans 26 status badges.

Modified proposal: Adopt the compact index and merged detail page with three amendments. Index: header sentence + search + group filter + compact rows, where each row keeps a one-line mono CLI template with a Copy button alongside name, status badge, one-line summary, and Open — and search continues to match aliases, tags, and CLI text even though rows no longer display them. Keep the WorkflowMap as the single orientation device; delete StatusCounts, TaskLauncher, ReferenceNotice, the goal-card guide, and the coverage dashboard, but keep one visible one-line legend above the list (Ready = full web flow · Partial = web builds the command, CLI executes · CLI only) instead of tooltip-only. Detail page: header + merged guidance block (Use When/Produces/Next Step + the Preselected-in-web/You-still-provide mapping, each stated once) + CLI form with copy + one-row workflow-position breadcrumb with Before/After links; drop CommandGoalContext, CommandUsePath, ConverterCommandBridge, and DirectConvertArtifactMap (their unique copy already exists on /convert and /donjon). On /builder: replace CommandContextPanel's three-card grid with a single compact line "Produces: {produces} · Next: {next_step}" under the WorkflowHint, keeping the Command guide button as the deep-dive path.

### MODIFY-PROPOSAL — Command strings are hand-maintained in six places and have already drifted: one naming convention, one generator

Proposal: Adopt one artifact-naming convention end to end (pick either openmc_sph.* or sph_sidecar.* and use it in canned CLIs, form placeholders, and catalog strings). Generate the SPH panel's canned step CLIs from the builder specs plus the same prefill params its 'Open step' hrefs already carry, instead of hand-writing them (after the /equivalence merge in finding 1, all six steps have specs to generate from). For the catalog, either derive DETAILS[].cli from the spec defaults at test time (a unit test asserting catalog cli == buildCommandCli(spec, defaults) would freeze the drift) or drop cli from index cards per finding 3 and keep only the detail-page copy.

Argument: The six "places" are not one feature duplicated six times, and the two the reviewer actually attacks have jobs a generator would do worse. The workflow panel's canned CLIs are editorial content whose value is filename CONTINUITY: step 01 writes openmc_ce_flux.h5, step 03 consumes 'openmc_ce_flux.h5::openmc_volume_flux' and writes openmc_sph.h5, steps 04/05 consume '--sph-source openmc_sph.h5', step 06 converts mgxs_with_sph.h5 — that chain IS the J3 teaching artifact, and the proposed "generate from builder specs plus the hrefs' prefill params" cannot produce it in this codebase: step 03's href is bare '/equivalence?kind=openmc-sph-sidecar' (no prefills), step 05 likewise, step 06 targets /convert which has no spec, and the proposal's own parenthetical admits it only works "after the /equivalence merge in finding 1" — it's a plan for a hypothetical codebase. Generation would also silently change the strings: buildCommandCli over CE_EXPORT_HREF's params emits '--summary-json openmc_ce_flux_summary.json' but drops '--mgxs mgxs_library.h5' (optional field, absent from the href), which the hand-written step 01 deliberately includes because the export needs it for mixture names/energy bounds. The catalog's 26 cli strings sit in Python (commands.py) while every builder is TypeScript; "derive DETAILS[].cli from spec defaults at test time" means codegen or booting FastAPI inside vitest — disproportionate machinery for one-line example cards, exactly what the owner's "不做多余的" forbids. BUT the drift itself is confirmed and user-hostile at the worst spot: on /equivalence?kind=openmc-sph-sidecar the panel's step 03 ('-o openmc_sph.h5 --table-output openmc_sph.csv', with CopyCliButton) renders on the same screen as the form preview defaulting to 'sph_sidecar.h5' / SPH_TABLE_OUTPUT_PLACEHOLDER 'sph_sidecar.sph.csv' (equivalence/page.tsx:38,599 vs OpenmcSphWorkflowPanel at page.tsx:158); an engineer who accepts the form default then copies step 04/05 gets a broken pipeline, and the catalog adds a third name, 'augmented.h5', for the same augment output that the workflow and form both call 'mgxs_with_sph.h5'. The repo's own house style already shows the right fix: pyganCompare.ts's header documents "the web run and the 'Same command' CLI preview must agree" as a UNIT-TESTED rule, and openmcSphWorkflow.test.ts / equivalenceCommand.test.ts exist. So: unify the names by convention and freeze parity with tests; keep the hand-authored strings.

Modified proposal: Adopt the naming convention, drop the generator, freeze with tests. (1) Standardize on openmc_sph.* for the OpenMC-side SPH artifacts — not sph_sidecar.*, because the apply-sph builder spec already defaults '--sph-source openmc_sph.h5' (commandBuilder.ts:299) and because 'sph_sidecar.h5' is also the placeholder for the DIFFERENT make-sph-sidecar command, so reusing it for make-openmc-sph-sidecar collides two commands' outputs. Concretely: in equivalenceCommand.ts set the openmc-sph-sidecar outputPlaceholder to 'openmc_sph.h5'; in app/equivalence/page.tsx set the table-output placeholder to 'openmc_sph.csv' for that kind; in commands.py fix make-openmc-sph-sidecar cli to '-o openmc_sph.h5 --reference-flux openmc_ce_flux.h5::openmc_volume_flux --mg-flux openmc_mg_flux.h5::openmc_mg_flux' and augment-sph cli to '--sph-source openmc_sph.h5 -o mgxs_with_sph.h5' (retiring 'augmented.h5'). (2) Do NOT generate the panel's canned CLIs from builder specs — the chain (steps 03/05/06 have no builder prefills to generate from, and finding 1 hasn't landed) stays hand-authored. Instead add parity tests in the files that already exist: in openmcSphWorkflow.test.ts assert (a) chain integrity — each step's output filename appears in the next consuming step's cli — and (b) step 03's cli equals buildEquivalenceCli(defaultEquivalenceOptions('openmc-sph-sidecar') with the chained flux sources filled in), which mechanically pins the panel to the form the moment either drifts; mirror the pyganCompare.ts pattern of tested CLI/preview agreement rather than inventing a generator. (3) For the Python catalog, add a pytest over DETAILS asserting the SPH-group cli strings use only the canonical artifact names (openmc_ce_flux.h5, openmc_mg_flux.h5, openmc_sph.h5/.csv, mgxs_with_sph.h5) — cheap, same-language, and it freezes future drift without cross-boundary codegen. Keep DETAILS[].cli itself: whether index cards show it is finding 3's question, not this one's.

### MODIFY-PROPOSAL — /pygan's 'Doctor CLI' button promises a builder and delivers the fallback

Proposal: Replace the button with an inline CopyCliButton for 'openmc2donjon pygan-doctor' next to the Doctor result panel (consistent with the page's web-run/CLI parity ethos), or link to /commands/pygan-doctor. One-line change; removes the only nav affordance in the app that routes to a builder that does not exist.

Argument: The affordance is needed — /pygan's design pairs every web-run with its CLI twin (compare form CLI preview at page.tsx:297, compare result 'Copy CLI' at page.tsx:541), and dropping it would leave DoctorPanel the only result on the page without CLI parity. But the routing is indefensible: the backend catalog itself names /pygan as pygan-doctor's web surface (commands.py:497 web_path="/pygan"), the command has nothing to build (one optional --summary-json flag), and this is the app's only /builder?command= link targeting a spec-less id — so the button ships users away from the command's own designated home to a page whose subtitle concedes "This command does not have a structured web builder yet." One correction to the finding: the destination is not broken — builderCopy.ts:24-29 renders the catalog CLI with a CopyCliButton and builderCopy.test.ts:28 tests the literal pygan-doctor id — so severity stays minor; the failure is a superfluous navigation, not a dead end. The proposal's second option (link to /commands/pygan-doctor) must be rejected: that guide's web pointer routes back to /pygan (a loop) and still discards the live doctor result the user is reading.

Modified proposal: In web/app/pygan/page.tsx:467-469, replace the Link with an inline copy button mirroring ComparePanel's established pattern: <CopyCliButton value="openmc2donjon pygan-doctor" label="Doctor CLI" /> (CopyCliButton is already imported at line 7). Do not link to /commands/pygan-doctor — the catalog already designates /pygan as that command's web surface, so the guide link would be a round trip. No other change; keep the "Fill mock compare" button and the fallback machinery in builderCopy.ts as-is (it is tested, load-bearing behavior for any future spec-less catalog id).

### MODIFY-PROPOSAL — Nine metaphors for "ordered sequence of commands", and four of them collide with filesystem "path"

Proposal: Two words only: "workflow" for any named multi-command sequence (it already matches the CLI noun openmc-workflow and the ?workflow= param) and "step" for items inside one. Reserve "path" exclusively for filesystem paths. Renames: "OpenMC-side SPH route" → "OpenMC-side SPH workflow"; "Main line" → "Main workflow"; "recommended demo path" → "Demo workflow"; "Handoff path after this result" → "Next steps" (or delete per the /convert dedup); "Main product paths" → "Start here"; drop "lane"/"pipeline"/"chain" headings entirely.

Argument: The synonym sprawl and the path collision are real (OutputActions.tsx alone stacks "Handoff path after this result", "delivery route", and "delivery command chain" beside actual filesystem-path fields), but collapsing to "workflow"-only erases a load-bearing semantic distinction: "route" in this product denotes a mutually exclusive physics/consumption BRANCH, not a command sequence. "MACROLIB NSPH route" (convertSphHandoff.ts:24) names the validated consume alternative versus MULTICOMPO-with-inert-metadata — the product's single most consequential caveat; "Accepted SPH consumption route: MACROLIB GROUP/*/NSPH" (openmcSphSummary.ts:281) is the accepted-validation credential itself, and `route` is a backend API field (api.ts:412) rendered verbatim in the physics summary card. Renaming "OpenMC-side SPH route" to "SPH workflow" would also collide with the literal `openmc-workflow` CLI noun and falsely imply the UI executes it, breaching the plan-only boundary. Engineers need two words for two questions: "which branch am I on?" (route) and "what do I run next?" (workflow).

Modified proposal: Adopt a three-term system instead of two: (1) "workflow" for any ordered multi-command sequence, matching the openmc-workflow CLI noun and ?workflow= param — rename "Main line" → "Main workflow", "recommended demo path" → "recommended demo workflow", "Workflow pipeline" → drop the word "pipeline" (heading is already under "workflow"), "Handoff pipeline" → "Handoff workflow", "delivery command chain" → "delivery workflow", "demo click path" → "demo workflow", "production lanes"/commandWorkflowLanes copy → "production workflows". (2) "route" reserved exclusively for mutually exclusive physics/consumption branches, matching the backend `route` field: KEEP "MACROLIB NSPH route", "OpenMC-side SPH route", "apply-sph route", "direct converter route"; and pull branch-choice copy INTO this term — "normal production path" (convertWriterBackend.ts:30, the built-in-vs-PyGan writer choice) → "normal production route" (convertPostWriteFocus.ts:40 already says "normal converter route" for the same choice), nav "after the main path is chosen" → "after the main route is chosen". (3) "path" exclusively filesystem: "Handoff path after this result" → "Next steps", "Main product paths" → "Start here", "safer handoff path" → rewrite plainly ("dry-run plus production checks is the safer way to hand off"). "step" stays the only word for items inside a workflow.

### MODIFY-PROPOSAL — inject / augment / apply / "corrected" — the SPH state vocabulary invites the exact double-application error the UI elsewhere guards against

Proposal: Fix the verbs to the CLI, which is the ground truth users execute: "augment" = attach ADF/NSPH records, XS unchanged (tabs → "Augment with ADF (augment-adf)" / "Augment with SPH (augment-sph)"; drop "inject" everywhere); "apply" = divide XS (apply-sph only). Ban "corrected" for the augmented file: "Send corrected MGXS to Convert" → "Send SPH-augmented MGXS to Convert"; reserve "SPH-applied" for apply-sph output. One sentence at the top of /equivalence can lock the distinction: "Augment attaches factors as records; apply rewrites cross sections."

Argument: The core hazard is confirmed and indefensible: "corrected" names both the augment-sph output (NSPH records attached, XS untouched — "Send corrected MGXS to Convert", OpenmcSphMainlineCard.tsx:60; "corrected HDF5 handoff", convertSphHandoff.ts:26) and the apply-sph output (XS actually divided — "SPH-corrected MGXS HDF5 copy", commandBuilder.ts:294), so the same screen that prints "applied_to_xs = false … were not silently multiplied in the HDF5" (OpenmcSphPhysicsSummaryCard.tsx:344-351) links a button calling that very file "corrected" — the vocabulary sabotages the product's own double-application guard. But the proposal overreaches twice. First, the tabs already display the CLI ground truth: EquivalenceTabs renders the mono commandId (augment-sph) inside every tab and again as the section eyebrow, so "the word they clicked never appears in the command they copy" is factually wrong, and the proposed "(augment-adf)" parenthetical would duplicate what the tab already shows. Second, banning "corrected" wholesale would delete textbook SPH physics language about flux (not files): "corrected MG flux matches the CE reference" (openmcSphSummary.ts:146) and "the macro calculation being corrected" (equivalence/page.tsx:474) state the SPH fixed-point condition and are validated substance. The right fix targets artifact names only and consolidates on the verb the body copy already uses everywhere ("attach").

Modified proposal: Adopt a three-term artifact vocabulary keyed to the CLI, applied to file naming only. (1) Augmented = records attached, XS unchanged: rename tab labels "Inject ADF"→"Augment ADF" and "Inject SPH"→"Augment SPH" (equivalenceCommand.ts:67,94; no parenthetical — the mono commandId is already rendered in each tab) and titles to "Augment MGXS with ADF/DF" / "Augment MGXS with SPH"; sweep the remaining "inject" strings to "attach"/"augment" (openmcSphWorkflow.ts:88 step title, commandWorkflowMapping.ts:163,166 plus its matcher in OpenmcCommandList.tsx:71,74, commandGoals.ts:73,75,98, taskEntrypoints.ts:25, openmcEntryPoints.ts:39, commandWorkflowLanes.ts:112, OpenmcSphWorkflowPanel.tsx:23, OpenmcProductionPathPanel.tsx:72, OpenmcWorkflowSummary.tsx:137, AsciiPreview.tsx:201). (2) Never call the augmented file "corrected": "Send corrected MGXS to Convert"→"Send SPH-augmented MGXS to Convert" (OpenmcSphMainlineCard.tsx:35,60,67; OpenmcSphPhysicsSummaryCard.tsx:90,189,193), "corrected HDF5"→"SPH-augmented HDF5" (openmcSphWorkflow.ts:103, convertSphHandoff.ts:26, convertIntent.ts:41,43, openmcSphDemo.ts:54,111, openmcSphSummary.ts:97, demoShortcuts.ts:41, OpenmcProductionPathPanel.tsx:97-98, CommandWorkflowMap.tsx:18, donjon/page.tsx:445), SphHandoffCard.tsx:38 tile "Corrected source"→"Augmented source". (3) Applied = XS divided, apply-sph only: commandBuilder.ts:294,300,302 "SPH-corrected"/"Corrected MGXS HDF5"→"SPH-applied MGXS HDF5" (matches the CLI's own --sph-applied flag and the footnote's "apply-sph route"). Explicitly KEEP "corrected" where it describes flux/physics rather than a file: equivalence/page.tsx:474,536 and openmcSphSummary.ts:146 (SPH fixed-point statements). Add the reviewer's one-line distinction lock to the /equivalence header: "Augment attaches factors as records — cross sections unchanged; apply-sph rewrites the cross sections."

### MODIFY-PROPOSAL — Nav labels, page H1s, and cross-links disagree on what each page is called

Proposal: Make nav label a prefix of the H1: "Inspect HDF5" / H1 "Inspect HDF5 handoff"; "OpenMC planner" / H1 "OpenMC planner — prepare the MGXS handoff" (and delete the other three self-names in lib/commandWorkflowMapping.ts and goal cards); "PyGan validation" / keep H1; "ADF/SPH sidecars" / H1 "ADF/SPH equivalence sidecars". Cheap, mechanical, and it makes word-of-mouth navigation work.

Argument: The finding is real and the /inspect half is indefensible — I grepped every occurrence: "review" names the inspect page exactly once (lib/navigation.ts:21) against 20+ "Inspect HDF5"/"inspector" strings, and worse, "Review" is already the app's established verb for post-run evidence checking ("Review production evidence", "Review the ASCII handoff", "Review or deliver"), so the nav label points the word at the one page that is pre-conversion inspection — a direct violation of "一眼就知道是什么". But the /openmc remedy is backwards: nav "OpenMC prep" and H1 "Prepare OpenMC handoff inputs" are already stem-consistent anchors (like "Convert"/"Direct converter"); the drift comes from three outlier "planner" strings, so the fix is to retire the outlier term, not rename the two most visible surfaces to match it. And "delete the other three self-names in commandWorkflowMapping.ts" misreads that file — its title/summary fields are workflow descriptions on /commands cards (parallel to "Direct converter workflow", "MGXS handoff inspection") carrying variant info, not competing page names; the only true outlier there is surface: "OpenMC planner" breaking the file's own "X page" convention (line 61).

Modified proposal: Adopt the rule "nav label is a stem of the H1; one name per page", applied minimally: (1) /inspect: nav "Review HDF5" → "Inspect HDF5" (lib/navigation.ts:21); keep H1 "Inspect HDF5 handoff" and all existing "inspector" cross-links — this also frees "review" for its app-wide post-run meaning. (2) /openmc: keep nav "OpenMC prep" and H1 "Prepare OpenMC handoff inputs" (already stem-aligned); retire the outlier term instead: "Loading OpenMC planner…" → "Loading OpenMC prep…" (app/openmc/page.tsx:80), commandGoals CTA "Open OpenMC planner" → "Open OpenMC prep" (lib/commandGoals.ts:38, and actionHint "Use the planner first…" → "Prepare the handoff first…"), and commandWorkflowMapping surface "OpenMC planner" → "OpenMC page" (lib/commandWorkflowMapping.ts:61), matching the file's own "Convert page"/"Inspect page" convention. Keep the mapping's title/summary sentences ("OpenMC production handoff planner…") but reword "planner" → e.g. "OpenMC production handoff workflow" — they are workflow descriptions on /commands cards, not page names; do not delete them. (3) /pygan: nav "PyGan option" → "PyGan validation"; keep H1 "PyGan writer validation" and keep the dropdown description "Optional backend diagnostics; ASCII remains default", which preserves the owner's ASCII-default positioning that the bare label change would otherwise lose. (4) /equivalence: keep nav "ADF/SPH sidecars" (engineers scan for the terms of art); H1 "Equivalence sidecars" → "ADF/SPH equivalence sidecars" (app/equivalence/page.tsx:129).

### MODIFY-PROPOSAL — The input file is referenced thirteen ways; "MGXS HDF5" already dominates

Proposal: Standardize field labels and headings on "MGXS HDF5"; allow "MGXS handoff" only in hero/explanatory prose where the handoff coinage is being taught; delete "converter-facing HDF5" and "source HDF5" except where a genuine source-vs-intermediate contrast exists (two-step planner), and there label the pair explicitly "Source MGXS HDF5" / "Intermediate (augmented) HDF5".

Argument: The convergence is right but the proposal is aimed at the wrong targets: its anchor example is false in code — the bundle form field is already labeled "MGXS HDF5" (lib/commandBuilder.ts:316), and "Intermediate HDF5" lives only on the /openmc planner (app/openmc/page.tsx:442) beside Recipe/Statepoint/Run-directory/Output-ASCII with no source-HDF5 field to pair it with, while the proposed "(augmented)" qualifier would be factually wrong since the planner's Direct mode keeps a plain, un-augmented export. Meanwhile the blanket "standardize headings on MGXS HDF5" would break /inspect, which deliberately says "HDF5 handoff" (page.tsx:241,264) because its job (J4) is to decide whether the file IS an MGXS handoff — Summary.tsx:222 renders "This HDF5 doesn't look like an MGXS handoff" as the diagnosis. The real, code-verified drift is elsewhere: the same CLI positional input_h5 is labeled "Input HDF5" on /convert (ConvertForm.tsx:146) but "MGXS HDF5" on /builder; the bundle pitch sentence "Collect {source|input} HDF5, ASCII output, summaries, and logs" appears four times with a 3/1 noun split; and "converter-facing HDF5" (3 uses) adds nothing "MGXS" doesn't already disambiguate from statepoint.h5.

Modified proposal: Standardize on "MGXS HDF5" for field labels naming the converter's input, with three role-based exceptions instead of location-based ones. (1) Rename /convert's "Input HDF5" label (ConvertForm.tsx:146) and its error/next-step strings to "MGXS HDF5" so /convert matches /builder and the bundle form for the same CLI argument input_h5. (2) Unify the four bundle pitch sentences (commandWorkflowLanes.ts:57, commandGoals.ts:116, commands/[id]/page.tsx:294, convertNextSteps.ts:197) on one wording: "Collect the MGXS HDF5, ASCII output, summaries, and logs…"; likewise convertDeliveryChecklist.ts:52 → "MGXS HDF5 accepted". (3) Replace all three "converter-facing HDF5" uses (OpenmcProductionPathPanel.tsx:158, OpenmcCommandList.tsx:69, openmcSphWorkflow.ts:91) with "MGXS HDF5". Keep "handoff" wherever contract membership is the subject — not only hero prose: the /inspect heading and aria-label ("Inspect HDF5 handoff", without "MGXS", since the page diagnoses whether the file is one), the inspect warning "This HDF5 doesn't look like an MGXS handoff", and contract verdicts like ConvertReportShared.ts:17. Leave the /openmc planner's "Intermediate HDF5" label as is — its contrast is with Recipe/Statepoint and Output ASCII, not with a source HDF5, and it is not necessarily augmented; do not add "Source MGXS HDF5 / Intermediate (augmented) HDF5" pair labels anywhere.

### MODIFY-PROPOSAL — Micro-inconsistencies: Dry run vs Dry-run, mixed eyebrow casing, Use when vs Use When

Proposal: Style rule: "Dry run" as noun/subject, "dry-run" only as modifier ("dry-run CLI"); collapse the six result phrasings into one shared string per state. Pick sentence-case for all eyebrows. "Use when" everywhere. These are one-afternoon greps, and they are exactly the surface the owner's "优雅整洁" bar is judged on.

Argument: Two of the three sub-claims mis-measure the rendered product, and the one collapse the proposal actually mandates would delete load-bearing guidance. (a) The "six-plus hand-written phrasings" are not duplicates of one string — after a dry run the convert page shows several panels at once, and each sentence pairs the shared fact ("no file written") with that surface's specific consequence: convertAsciiReadiness.ts:50 carries the overwrite gate ("Convert can overwrite this target only when the overwrite option is enabled" — stated nowhere else), convertNextSteps.ts:138 names the button and output path, convertDeliveryChecklist.ts:107 names the artifact the checklist tracks, convertDecision.ts:83 is an audit-reason bullet. Collapsing "into one shared string per state" either deletes the overwrite condition or prints the identical sentence 3-4 times on one screen — both worse for the owner's 干干净净 bar than varied context-specific copy. Only the subject hyphenation ("Dry run did not write" vs "Dry-run did not write") is a real defect. (b) The eyebrow-casing claim is invisible in the product: every element that is actually an eyebrow ("recommended demo path", "Main line", "delivery command chain", /donjon's "object"/"mapping"/"solver", "Bundle") renders through `text-[10px] uppercase tracking-[0.14em]` — users see uniform ALL-CAPS regardless of source casing. And "Technical evidence" (a details/summary header, OutputActions.tsx:218) and "Production evidence" (an h3, ConvertRunDetails.tsx:188) are not eyebrows at all; they are headings, already consistently sentence-case with each other. The claim that this is "exactly the surface the 优雅整洁 bar is judged on" fails: that bar is judged on rendered pixels, which are already uniform. (c) "Use When" vs "Use when" is real and user-visible across pages for the same catalog field — but the naive fix creates a new defect: on /commands/[id] the card is one of a title-case trio "Use When / Produces / Next Step" (page.tsx:131-133); changing only the first leaves "Next Step" inconsistent two cards away. So: fix the two genuinely user-visible terms (hyphenation rule + sentence-case the whole ExplainerCard trio), keep the distinct per-surface sentences, drop the eyebrow item as rendered-invisible.

Modified proposal: Scope to the two user-visible defects, preserve per-surface content: (1) Adopt the term rule — "dry run" (open, two words) as noun/subject, "dry-run" only as modifier ("dry-run CLI", "--dry-run") — and apply it to the ~8 user-facing strings that violate it (convertDeliveryChecklist.ts:107, convertNextSteps.ts:138, OutputActions.tsx:395 "Dry-run next step" and :398, convertSphHandoff.ts:31/39, MockDemoCard.tsx:53 "Run demo dry-run" → "Run demo dry run", ConversionSummaryStrip/convertRunSummary status values). Do NOT merge the sentences: each panel keeps its own second clause (overwrite gate, button+path, checklist artifact, audit reason) — only the subject spelling unifies. (2) On /commands/[id] page.tsx:131-133, sentence-case the whole ExplainerCard trio: "Use when" / "Produces" / "Next step", matching builderCopy.ts and CommandCards.tsx. (3) Drop the eyebrow-casing item from the user-facing fix list — all true eyebrows are CSS-uppercased so rendered output is already uniform, and "Technical evidence"/"Production evidence" are headings already in consistent sentence case; normalizing source casing may ride along in the same commit as a zero-risk tidy, but it is not part of the design bar.

### MODIFY-PROPOSAL — /commands' first screen tells the evaluator the product is unfinished and to leave — contradicting home's validation credential

Proposal: Delete the READY/PARTIAL/PLANNED counter tiles from the header (the per-card status badges with a tooltip legend carry the same information where it is actionable), keep exactly one redirect device (the existing header sentence 'For normal work, start with Convert'), remove the duplicated TaskLauncher and amber notice, and let search + the first command group start on screen 1.

Argument: The three attacked devices are genuinely redundant duplicates, so the deletions are right — but the proposal keeps a redirect with no click affordance and its stated outcome is impossible without gutting the page's real substance. Evidence from code: (1) The StatusCounts tiles render raw internal vocabulary ("PARTIAL") in marquee position, while the exact same counts already live in the collapsed "Web command coverage" dashboard WITH the StatusLegend that defines Partial as "Planner/viewer/builder: the web UI prepares the command… production file mutation stays in the CLI" — the tiles are the legend-less copy of a section that exists 300px lower; no engineer job needs aggregate status counts, and the per-card badges already show human labels ("Command builder ready", "Available inside Convert"), not the word "partial" (the reviewer's "tooltip legend" claim is wrong — there is no tooltip — but the badges are self-labeling, which is stronger). (2) The TaskLauncher renders the identical TASK_ENTRYPOINTS array as home's "Start here"; a /commands visitor passed that content one click ago, and nav's first item is Convert on every page. (3) The page currently carries FIVE redirect devices (header sentence, TaskLauncher, amber notice with 2 buttons, FeaturedCommand's "Open converter", nav) — three apologies before any content is itself what makes the page read as unfinished. However, two corrections are decisive: first, the header sentence "For normal work, start with Convert" is plain text — if TaskLauncher and the notice both go, the only clickable escape is the nav; the vacated tiles slot should hold one "Open converter" button so the single surviving redirect actually works in a demo. Second, the proposal cannot deliver "search + the first command group on screen 1": between header and filters sit "Choose by user goal" (6 cards) and "Workflow map" (3 lanes), and those must stay — the goal guide is the intent→command index a reference page should open with (better than a raw search box for 26 jargon-named commands), and the workflow map carries the load-bearing product boundary ("openmc2donjon carries the resulting NSPH factors; it does not run a DONJON feedback loop") that matches home's advertised "Reference map for every CLI command". The right target is "goal guide on screen 1", not "search on screen 1". Finally, the reviewer's own belief-flip concern survives their fix: the Status filter row still shows a bare "Partial" button to every cold visitor; rename it to "Builder" to match the GoalStatusPill's existing "N builder" wording.

Modified proposal: In CommandWorkspace.tsx: (1) delete StatusCounts and render one primary "Open converter" Link (href /convert?intent=direct-convert&format=multicompo&check=1&production=1) in the header slot it occupied, keeping the existing subtitle sentence as the only redirect copy; (2) remove the TaskLauncher block ("Main product paths") entirely; (3) remove ReferenceNotice ("ADVANCED PAGE / This is not the main converter flow") entirely — its two buttons are covered by the new header button plus the goal guide's per-goal CTAs; (4) keep GoalCommandGuide ("Choose by user goal") as the first content section — it, not search, is the correct screen-1 for a reference page — and keep WorkflowMap and the collapsed "Web command coverage" details unchanged (the legend there is where "Partial" is defined). In CommandFilters.tsx: rename the Status filter option label "Partial" to "Builder" (value unchanged), aligning with GoalStatusPill's "N builder" vocabulary so the internal word "partial" never appears above the fold anywhere on the page. Net result: screen 1 = title + one sentence + one button + the goal index; roughly 1100px of duplicated preamble removed; first command group moves up ~2 viewports without touching the page's validated reference substance.

### MODIFY-PROPOSAL — /convert contradicts the 'squeaky clean' promise: actions 1.65 screens down, post-run report 4+ screens

Proposal: Adopt the demo card's inline-status pattern as the report's model: make the primary outcome card the sole action surface, keep Validation summary + one gates section + one delivery checklist visible, and demote everything else into the already-existing collapsed details. Suppress the intent banner when its text duplicates the page header (the generic 'Direct conversion' variant). This matches the convert-page reviewer's cut list; my walkthrough confirms the user cost is real at 900px.

Argument: The scroll cost is real, but the proposal crowns the wrong card and cuts the wrong copy of the duplicated content. The primary outcome card ("ASCII handoff ready") is computed from the convert response alone; the "Output ready" section it would demote is the only layer that cross-checks that response against the live filesystem (fetchConvertArtifactStatuses, "Refresh file status", and the dedicated conflict state "verify output path" when convert reports a write the probe cannot see) — making the response-only card the sole action surface would let the UI declare an artifact ready that does not exist on disk, which for a converter whose whole pitch is "checked handoff" is a substance loss, not a cleanup. Meanwhile the collapse architecture the proposal asks for already exists: ConvertRunDetails is open={!data.ok}, so Run details / CLI equivalent / Handoff pipeline / Artifact anatomy / Production evidence / Production checks / Input details / Delivery checklist / Production handoff next steps are all behind one collapsed details on the success path and auto-open precisely on failure — the reviewer's ~40-heading count conflates DOM presence with visible scroll. The genuine visible bloat is exactly two duplications the proposal under-specifies: (1) the action verbs rendered twice (primary card AND the OutputActions state card) plus a third "Convert now" in the demo card, and (2) buildGates() rendered twice, with "Gate highlights" visibly duplicating inside a Validation summary whose own copy promises "technical gate evidence is kept in the details section". So the correct move is merge-and-dedupe — fold the file-status probe, delivery paths, and blocked/conflict logic INTO one probe-backed action card per run state, and delete the visible Gate highlights (keeping the collapsed Production checks that auto-open on failure) — not demotion of the verification layer. The SphHandoffCard must stay visible when triggered: it renders only when sph_calculations > 0 and carries the validated NSPH routing warning (MULTICOMPO NSPH is inert metadata DONJON NCR: does not consume; use MACROLIB), the one mistake that silently produces a wrong core calculation. Banner suppression is right and should extend to the "Direct conversion" variant since nav and home both link with ?intent=direct-convert, making the self-duplicating banner the default first impression; the check and openmc-sph variants carry deep-link physics guidance and stay.

Modified proposal: Scope: success path only; failure path (BlockedOutputActions triage + auto-opened Run details) is untouched. (1) Merge OutputActions into ConvertOutcomeSummary so each run state has ONE status-bearing action card: move the artifact-status strip (convertArtifactStatusSummary + "Refresh file status"), the conflict/"verify output path" logic, and the delivery-path tiles (ASCII handoff, bundle dir, manifest) into the primary card; keep its "Advanced delivery evidence" details as that card's single collapsed annex. This removes the duplicated Preview ASCII / Copy CLI / Bundle rows and the second/third "Convert now" without losing the filesystem cross-check. (2) Delete ProductionGateHighlights from ConvertValidationSummary — keep the six validation tiles + first issue/warning preview; gates render once, in Run details' CheckCards, which already auto-opens on failure (honoring the summary's own copy: "technical gate evidence is kept in the details section"). (3) Keep SphHandoffCard visible when sph_calculations > 0 (validated NSPH-route contract warning); keep AsciiPreview. (4) Suppress ConvertIntentBanner for both "generic" AND "direct-convert" intents (nav/home link with ?intent=direct-convert, so it is the default duplicate); fold its one non-redundant line ("Start with Dry run, then write") into the page subtitle; keep the "check" and "openmc-sph" banners, whose bodies carry non-header guidance (MACROLIB→GROUP/*/NSPH routing). Net: successful-run report = one outcome+delivery card, one validation-tiles card, conditional SPH card, one collapsed details, preview — roughly two viewports instead of seven, with every cut item being a second render of data that survives elsewhere.

### MODIFY-PROPOSAL — Nav labels and landing identities disagree, so the evaluator's belief shifts on every click

Proposal: Rename nav 'Review HDF5' to 'Inspect HDF5' (matching the page and the home card); point 'OpenMC prep' at plain /openmc with no equivalence preselection and let the page's own two entry cards do the triage; on /builder cut the 'What this builder is for' panel and the workflow-strip chrome (per the builder reviewer) so the first path field is above the fold under the headline that promises inputs.

Argument: The diagnosis is verified in code, but fix (2) as written silently regresses the production contract: /openmc initializes production via queryFlag(searchParams,"production",false) (app/openmc/page.tsx:111) and parseOpenmcWorkflow(null)="one-step", so "plain /openmc" would land every nav click with production gates OFF — trading a wrong SELECTED chip for a wrong safety default on the page whose whole job is planning a production handoff. Only equivalence=sph is the offender; production=1 and workflow=two-step are the direct-mgxs entry point's own defined defaults (lib/openmcEntryPoints.ts:29-33), and dropping just the sph param makes activeOpenmcEntryPoint select "Prepare OpenMC MGXS HDF5" — exactly what the label "OpenMC prep" promises. Likewise fix (3) must not sweep away BundlePrefillPanel: its prefilled state is the Convert→Bundle J5 receipt (artifact chips, manifest path, "Prepare validation" link into validate-bundle, DONJON guide link — lib/builderPrefill.ts:35-57); the cuttable fold-eaters are CommandContextPanel (duplicates the /commands/[slug] guide one click away, a redundancy builderCopy.ts's own comment already flags), the WorkflowHint strip, and the generic 3-card BUNDLE_WORKFLOW_STEPS explainer.

Modified proposal: (1) Rename nav "Review HDF5" to "Inspect HDF5" in lib/navigation.ts:21 — accept as proposed; it is the only "Review" in the product against 11 "Inspect" strings. (2) Change the nav href to /openmc?workflow=two-step&production=1 (drop only equivalence=sph, keep production gates and two-step). Landing then shows the "Prepare OpenMC MGXS HDF5" card SELECTED — matching the "OpenMC prep" label — with the SPH card one click away, and production gates stay on. Leave the home "Start here" card #2 href untouched: it says "Prepare CE/MG SPH" so its equivalence=sph preselection is honest. (3) On /builder, keep the header, the one-paragraph non-mutating disclaimer, and BundlePrefillPanel's status/chips/manifest/validation/DONJON links; cut CommandContextPanel ("What this builder is for" — the adjacent "Command guide" button already links to the same content on /commands/[slug]), cut the WorkflowHint strip, and cut the 3-card BUNDLE_WORKFLOW_STEPS strip from BundlePrefillPanel in both states (the panel's substance is the prefill receipt, not the generic steps). That removes roughly 450px of orientation chrome and puts the first path field under the "Fill inputs, copy CLI, run locally" headline without breaking the Convert→Bundle→validate-bundle→DONJON chain.

### MODIFY-PROPOSAL — J3 command-building is split across two parallel, differently-coded builder surfaces (/equivalence and /builder)

Proposal: Merge into one builder surface. Move the 5 equivalence forms into commandBuilder.ts specs so /builder?command=X hosts all 19 commands with one form engine, one saved-prefix pattern, one CLI-preview aside; keep /equivalence as a redirect (/equivalence?kind=adf-sidecar → /builder?command=make-adf-sidecar) to preserve every existing deep link. Replace the two More entries 'ADF/SPH sidecars' and 'Bundle' with 'Command builder' (landing on a kind-grouped index) plus a retained 'Bundle' deep link if J5 packaging deserves its own label. This also dissolves the reviewers' cross-page duplication findings (three mounts of OpenmcSphWorkflowPanel, conflicting artifact naming between canned and form-built CLIs) as a side effect of having one surface.

Argument: The two surfaces are not accidental twins; they are two deliberate layers, and the merge would trade a real but small stitching gap for engine complexity and regression risk on freshly verified physics emitters. First, the split is functional, not historical: the 5 /equivalence commands are the only mode-branched forms in the product — AdfSidecarFields swaps its field set on unity vs flux-ratio, SphSidecarFields on unity/macrolib/table, and the emitters scope flags by mode and suppress defaults (buildOpenmcSphSidecarCli emits --sph-target only when "rate", --zero-flux-policy only when "identity"). commandBuilder.ts's BuilderField has no conditional-visibility primitive and buildCommandCli emits any non-empty select value, so "move the 5 forms into specs" is not a data move — it either renders mode-irrelevant fields side by side ("Unity value" next to "Heterogeneous face flux", exactly the flag-mistake hazard the page's amber caveat exists to prevent on physics inputs) or grows the shared engine with a conditional system used by 5 of 19 commands, re-opening emitters that were just validated in the 58-defect campaign and carry dedicated tests (equivalenceCommand.test.ts). Second, the proposal's central UX claim is false for the validated mainline: the SPH chain crossing /builder→/equivalence→/builder→/equivalence→/convert IS connected by a stepper — OpenmcSphWorkflowPanel is mounted on every step's page on both surfaces with active-step highlighting, prefilled hrefs (CE/MG export deep links pre-set tally/dataset names) and canonical CLIs; the "three mounts" the reviewer counts as duplication are that stepper doing its job. Third, /builder is not "the J3 surface with no nav identity": 6 of its 14 specs are not J3 at all (diff, compare-writers, bundle, validate-bundle, doctor, serve); it is the form-detail view of the first-class "Commands" nav entry (every catalog card links to its web_path), which is ordinary index→detail IA. Merging would dissolve the one page where the whole J3 decision space is visible at a glance — five tabs, shared MGXS input carried across kind switches (equivalenceOptionsForKindSwitch, matching the real build-sidecar-then-inject sequence), accepted-evidence caveat inline — into a 19-command grab bag, against the owner's "一眼就知道是什么,怎么做". What survives of the finding is real and smaller: the ADF flux-ratio chain has only textual stitching (matching default filenames plus notes like "Use this before make-adf-sidecar --mode flux-ratio") instead of an on-page stepper, the field primitives visually diverged between the surfaces, and the sph-sidecar step's canned CLI (-o openmc_sph.h5) disagrees with the /equivalence placeholder (sph_sidecar.h5). Fix those three; keep the layering.

Modified proposal: Keep /equivalence and /builder at their current altitudes (job-shaped J3 decision surface vs generic per-command detail view of /commands) and make three targeted changes instead of a merge: (1) Add an AdfWorkflowPanel mirroring OpenmcSphWorkflowPanel — a lib/adfWorkflow.ts step list (export-surface-flux → make-low-order-driver / check-low-order-driver → make-homogeneous-face-flux → check-face-flux → make-adf-sidecar → augment-adf → convert, reusing the lane already defined in commandWorkflowLanes.ts "ADF/DF sidecar equivalence") mounted on the ADF-chain /builder commands and on /equivalence kinds adf-sidecar and augment-adf, with active-step highlighting and prefilled hrefs, so the mid-chain alternation between surfaces is guided exactly the way the SPH chain already is. (2) Extract the duplicated field primitives (PathField/TextField/SelectField/Toggle in app/equivalence/page.tsx vs BuilderFieldControl in app/builder/page.tsx) into one shared component set so the two surfaces have identical field look, Browse behavior, and CLI-preview aside — killing the "different layouts, field primitives" symptom without unifying the form engines. (3) Align artifact naming between canned and form-built CLIs: make the /equivalence openmc-sph-sidecar placeholders match the stepper's canonical names (openmc_sph.h5 / openmc_sph.csv in lib/openmcSphWorkflow.ts) or vice versa, one name per artifact everywhere. Optionally rename the More entry "ADF/SPH sidecars" to "Equivalence sidecars" to match the page's own H1. No route changes, no redirects needed, every existing deep link (openmcSphWorkflow steps, commandGoals, commandWorkflowLanes, home, ConvertShowcase) keeps working, and the five mode-branched emitters stay in their tested form.

### MODIFY-PROPOSAL — /settings does not earn a route: an entire top-level page and nav slot for one text field

Proposal: Retire the route and the nav item. Fold the preference into the point of use: next to every existing 'Use saved prefix' link, add its inverse — a small 'Set as default prefix' action on the path inputs (the six consumer pages already have the hook wired via useSettings). Keep the one-sentence privacy note ('stored only in this browser's localStorage') as the affordance's tooltip. Drop 'Restore defaults' entirely. Net: 10 routes → 9, More menu loses its weakest item, and the preference becomes discoverable exactly where it is used instead of on a page nobody visits twice.

Argument: The cut assumes the preference can be captured at its point of use, but the code shows it can't: the saved value is a hand-authored directory prefix (placeholder "/shared/you/openmc-runs/") that feeds placeholders, "Use saved prefix" fills, and the file browser's start directory (initialPath={pickBrowserStart(path.trim() || savedPrefix)}), while the consumer inputs hold full .h5 file paths — so an inline "Set as default prefix" either saves a filename as everyone's start directory or must guess directory depth. The route is also the only place the value can be viewed ("Current value: … (none)"), edited exactly, and cleared ("Leave blank to disable"); the proposal can set but never show or unset. And the trade is backwards for the owner's bar: it removes one quiet page in the last More-menu slot and adds six persistent-state write controls with feedback and tooltips to the working pages — including the primary Convert form. A settings page nobody visits twice is a settings page working as intended. The review's one real hit is "Restore defaults", which is behaviorally identical to clear+Save.

Modified proposal: Keep the /settings route and its More-menu entry as the single place to author, view, and clear the saved prefix (the value is a typed directory prefix, not derivable from any point-of-use input). Apply two trims the review surfaced: (1) delete the "Restore defaults" button — clearing the field and pressing Save is identical; (2) tighten the nav description from "Local browser preferences and default paths" to name the one thing it does, e.g. "Default path prefix for path inputs and the file browser." Do not add per-page "Set as default prefix" controls — six inline write affordances on working pages, including the primary Convert form, is strictly more chrome than the page they would replace.

### CUT-STANDS — Nav 'OpenMC prep' deep-links every click into the SPH (J3) configuration, leaving J2 — a named product job — with no clean route

Proposal: Change the nav href to the page's own default (drop equivalence=sph, i.e. '/openmc?workflow=two-step&production=1' with equivalence=direct), so nav lands on the direct-mgxs entry point; the SPH configuration remains one click away via the page's own 'Plan CE/MG SPH route' preset and the home 'Start here' card 2 (which correctly pre-sets sph+macrolib). Optionally retitle home card 2's eyebrow area or add a line so /openmc's dual identity (HDF5 export AND SPH planning) is visible from the first screen — the card body from openmcEntryPoints.ts ('Start here when your input is an OpenMC recipe/statepoint…') is already written and could be reused verbatim.

Argument: The nav's `equivalence=sph` deep-link has no constituency the code doesn't already serve better, and it contradicts the page's own canon. Showcase/demo needs are covered by a strictly superior link — HOME_DEMO_SHORTCUTS' "openmc-sph-minicase" carries format=macrolib, the summary= prefill, and the #openmc-sph-summary anchor, while the nav version lands the "OpenMC SPH physics summary" card empty; J3 self-selection is covered by home card 2 ("Need SPH first") and the page's own "Plan CE/MG SPH route" button. Meanwhile every in-page signal declares direct-mgxs the default entry: OPENMC_ENTRY_POINTS order, openmcEntryPoint()'s fallback to index 0, activeOpenmcEntryPoint() defaulting to "direct-mgxs", the header listing HDF5 export first, and direct-mgxs's body "Start here when your input is an OpenMC recipe/statepoint and you still need the converter-facing MGXS HDF5" — so a generic label ("OpenMC prep") deep-linking into the non-default heaviest mode marks a card "selected" that the user never selected, directly violating the owner's "一眼就知道是什么怎么做" bar. The defense also uncovered that the link is destructive, which seals it: the re-sync effect in app/openmc/page.tsx (lines 192–213) applies URL params on same-route navigations, so a J2 user mid-way through the "Plan HDF5 export" form is forcibly flipped back to SPH+macrolib by re-clicking the nav item that names their job. The proposed href is verified safe: parseOpenmcEquivalence(null) resolves to "direct" and parseConvertFormat(null) to "multicompo", so '/openmc?workflow=two-step&production=1' lands exactly on the direct-mgxs entry point; dropping the param (rather than passing equivalence=direct) is the correct form because the null-guarded re-sync then leaves an SPH user's in-page choice untouched. Nothing load-bearing breaks: navigation.test.ts asserts labels only, and all genuinely SPH-scoped deep-links (taskEntrypoints, demoShortcuts, openmcEntryPoints secondaryHref, CommandWorkspace's correctly-labeled "Prepare SPH inputs") live elsewhere and keep their sph params.

### MODIFY-PROPOSAL — /pygan: keep the route, keep it in More — do not fold it into /convert

Proposal: Keep /pygan as a route and as a More item. Trim within the page per the pygan reviewer (collapse the two-card overview into the header; fix the 'Doctor CLI' button that lands on /builder's spec-less fallback). Keep exactly two cross-references elsewhere: /convert's WriterBackendSelector link (already exists, correct point of use) and the More entry. With DONJON promoted, /settings folded, and /equivalence merged, More becomes: Commands, Command builder, Bundle, PyGan — four items, all genuinely secondary, and the 'optional' disclaimer can then live once on /pygan itself instead of six times across the app.

Argument: The route is load-bearing, not chrome: /pygan is the only surface that executes pygan doctor and compare-writers, and its ComparePanel is real acceptance evidence — PASS/FAIL over parsed LCM trees with rtol/atol, per-path differences, and the honest "Mode: mock fixture / live PyGan" disclosure (app/pygan/page.tsx:547). So keep-route-in-More is correct. But the proposal's "keep exactly two cross-references (WriterBackendSelector link + More entry)" would sever the single best-designed integration in the PyGan story: after a PyGan-backed convert, lib/convertPostWriteFocus.ts:22 shows "Validate the PyGan writer against the default ASCII writer… Before delivery, run the writer comparison" and lib/convertNextSteps.ts:62 deep-links to /pygan with input_h5, format, summary_json, and keep_dir prefilled — the page's nine searchParams exist precisely to receive it. That link is conditional (renders only when writer_backend === "pygan", i.e., exactly when validation evidence is owed) and turns J1→J6 into one click instead of re-typing four paths; cutting it to satisfy a reference count is the same wrong minimalism the proposal itself warns against. Two further prescription fixes: (a) the right in-page trim is not "collapse both overview cards into the header" — the PyGan card's live availability chip duplicates DoctorPanel's per-module found/missing grid on the same screen, while the ASCII card ("Open converter"/"CLI help" buttons on a page nobody reaches before knowing about /convert) is the pure dead weight; delete the ASCII card, merge the availability line into DoctorPanel, keep the header paragraph as the page's one disclaimer. (b) the "Doctor CLI" button (app/pygan/page.tsx:467 → /builder?command=pygan-doctor) has no builder spec (lib/commandBuilder.ts only defines "doctor" = openmc2donjon doctor, a different command) and a parameterless doctor never deserves a builder — replace it with an inline CLI string + CopyCliButton, matching the page's existing CLI-preview pattern. Also keep DoctorPanel's error-state reassurance "The default ASCII converter is still usable" — a disclaimer at the moment of failure is the one placement that earns its keep; the ones to strip are the nav description and home-card sentence (lib/navigation.ts:54, app/page.tsx:204).

Modified proposal: Keep /pygan as a route and as a More item; do not fold into /convert. In-page: delete the "Built-in ASCII writer" card of WriterBackendOverview entirely (header already states ASCII is default; its two nav buttons are superfluous), and fold the PyGan availability chip + missing-modules line into DoctorPanel so one section owns environment status; replace the "Doctor CLI" button's /builder?command=pygan-doctor link (spec-less fallback) with an inline copyable CLI string on the page itself — no builder spec needed for a flag-less command. Cross-references: keep THREE, not two — (1) /convert WriterBackendSelector's "PyGan status" link (point of choice), (2) the conditional post-write "Validate PyGan comparison" deep link with prefilled compare form (lib/convertPostWriteFocus.ts + convertWriterCompareHref — point of need, renders only after a PyGan-serialized write), (3) the More entry. Disclaimers: reduce to three earned placements — the /pygan header paragraph (canonical), WriterBackendSelector's one sentence on /convert (point of use), and DoctorPanel's error-state "The default ASCII converter is still usable" (reassurance at failure). Strip the apology from lib/navigation.ts:54 (description becomes plain, e.g. "PyGan writer diagnostics and ASCII-vs-PyGan comparison"), from the home Advanced-tools card body, and from the compare-section's "not a prerequisite" sentence.

### KEEP-FEATURE — /commands is the right route but its per-card Copy-CLI competes with /builder as a second command-construction surface

Proposal: On cards whose command has a structured builder (14 today, 19 after the merge), make 'Open builder' the primary action and demote the static CLI to a reference line without a copy button (keep Copy CLI only for genuinely CLI-only entries). The catalog stays the 'what is this command' authority; /builder stays the single 'give me a runnable command' authority.

Argument: The proposal's premise — that /builder is the "runnable command" authority and the catalog CLI is a stray duplicate — is contradicted by the code on both ends. (1) /builder is itself a copy surface, not an execution surface: its own header says "Builders are non-mutating... it only makes the CLI explicit" and its preview panel says "Copy this exact command into your shell" (web/app/builder/page.tsx). Both surfaces end in paste-into-terminal; they differ in job, not authority. The card's CLI is a synopsis — the man-page role — while the builder is an instantiator with real paths and dependency checks (builderCliIssues). (2) The demotion the proposal asks for already shipped: on every builder-backed card in CommandCards.tsx, the builder IS the primary action — "Open" renders btn-primary from web_path (/builder?command=X or /equivalence?kind=X), while Copy CLI is the compact 11px btn-secondary, and the card additionally carries the "Command builder ready" status badge and the "Web workflow: Command builder — Copyable CLI command builder" mapping hint. The only net change left in the proposal is deleting the copy button — and that harms a real user without fixing the claimed risk. The placeholder filenames are not junk: they are the pipeline's canonical artifact names that chain across cards (export-volume-flux writes openmc_ce_flux.h5, which is exactly the --reference-flux argument printed on the make-openmc-sph-sidecar card, whose sph_sidecar.h5 output is the --sph-source on the augment-sph card). The engineer scripting the OpenMC SPH iteration loop — which the catalog itself says to "repeat until the OpenMC-side SPH factors stabilize" — copies 3-4 chained templates into a shell script and does one rename pass; the builder is strictly worse for this because builderValuesFromQuery gets no prefill from card links, so every path must be re-picked through FileBrowserModal (backend-dependent) on every visit. Meanwhile the proposal keeps the CLI text visible as "a reference line," so placeholder text remains copyable — now by hand-selecting a whitespace-nowrap line inside an overflow-x-auto box, which is where paste errors actually come from. The failure mode being prevented is a reactor-physics engineer getting an instant file-not-found on a placeholder path — an audience that reads command synopses daily. The page's own charter ("Use Commands when you need to inspect a CLI flag, copy a lower-level command, or debug a workflow") names copy as its job; one small secondary button per card is not an IA seam, it is the feature. If anything sharper is wanted, rename the card's primary button from "Open" to "Open builder" — a label tweak, not a demotion of Copy CLI.

### MODIFY-PROPOSAL — Target IA: 4 primary nav items, 4 More items, 8 routes (from 3+6 nav / 10 routes), all six jobs reachable at their true rank

Proposal: Nav: Convert | OpenMC prep | Inspect | DONJON | More(Commands, Command builder, Bundle, PyGan). Routes kept, each with its one sentence of purpose: '/' — triage which of the lanes fits, show backend health and the accepted-validation credential; '/convert' — turn an existing MGXS HDF5 into checked L_MULTICOMPO/L_MACROLIB ASCII (J1); '/openmc' — plan the OpenMC-side commands that produce the MGXS HDF5 and, when needed, the CE/MG SPH evidence (J2+J3 planning; nav lands on the HDF5-export default); '/inspect' — read and verify any MGXS HDF5 down to per-mixture cross sections and scatter moments (J4); '/donjon' — generate the editable deck skeleton and run commands that consume the ASCII handoff (J5, promoted); '/builder' — fill inputs and copy the exact CLI for any auxiliary command: sidecars, flux exports, diff, bundle, diagnostics (J3 prep + J5 packaging + J6, absorbing /equivalence's five forms); '/commands' (+[slug]) — advanced reference for what each CLI command is for and where it runs on the web; '/pygan' — optionally prove the ASCII and PyGan writers agree (J6). Routes cut: /settings (preference folded to point of use), /equivalence (redirect into /builder). Net: every job has exactly one home, the nav reads as the pipeline the hero paragraph describes, and nothing validated or load-bearing is lost — which is the owner's '干干净净，一眼就知道是什么、怎么做' applied to the map rather than the pages.

Argument: The proposal's genuine finding is rank inversion in the nav, and every part of that is fixed by re-ranking — three edits to PRIMARY_NAV_ITEMS/SECONDARY_NAV_ITEMS in lib/navigation.ts. Its two route cuts, however, confuse "fewer routes" with "cleaner" and would delete engineered, tested, physics-load-bearing structure for a metric no user experiences: (1) /equivalence's five sidecar builders are mode-conditional forms (ADF unity vs flux-ratio swaps ~6 fields, SPH unity/macrolib/table swaps sources, the CE/MG SPH form's 12 controls carry the validated rate-target and zero-flux-policy physics) that the flat BuilderField engine literally cannot render — spec.fields.map shows every field unconditionally — so "absorbing" them means either always-visible irrelevant fields (the opposite of 干干净净) or rewriting the builder engine with conditionals, family tabs, and the tested cross-kind input carryover that builder's query-driven state reset would otherwise destroy; that is rebuilding /equivalence inside /builder right after the 58-defect stabilization, for zero user-visible gain. (2) /settings' single string is consumed by six pages (Inspect, Convert, PyGan, Equivalence, Builder, OpenMC) as placeholder, prefix-fill, and browser start dir — "folding to point of use" touches six surfaces to remove one 122-line page and one menu row, redistributing complexity rather than reducing it. The owner's bar "一眼就知道是什么怎么做" is about the map reading as the pipeline, which the nav re-rank alone achieves; route count was never the substance.

Modified proposal: Adopt the re-rank, keep the routes. (a) Primary nav: Convert | OpenMC prep | Inspect | DONJON — change the "OpenMC prep" href from /openmc?workflow=two-step&equivalence=sph&production=1 to plain /openmc so J2 lands on export planning with neutral defaults (the SPH preset stays on the home "Need SPH first" card, whose eyebrow earns it); relabel "Review HDF5" → "Inspect" for consistency with the page title and every cross-link's "Open inspector". (b) More: Commands, Command builder (/builder?command=diff — giving the 14-form surface a nav identity), ADF/SPH sidecars, PyGan, Settings; drop only the separate "Bundle" row, since it is a deep link into the same /builder page and bundle's real entry point is the post-convert "Package the production record" next-step. (c) Reject the /equivalence→/builder merge and the /settings cut for the reasons above. Net: 4 primary + 5 More, 10 routes unchanged, every job at its true rank, nothing validated or engineered rebuilt or lost.
